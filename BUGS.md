# Bugs

Alle bugs registreres her så snart de opdages. Opdateres når de fikses.

**Format**: `[status] YYYY-MM-DD — Titel` — beskrivelse, berørte filer, løsning (hvis fixed).
**Status**: `open` · `investigating` · `fixed`

---

## Åbne

- `[fixed 5.3.31 build 0347] 2026-05-16 — MnT-beriget data slettes ved næste STOMP-event` — `_handle_message_body` kaldte `cache.upsert(info)` med en frisk `SessionInfo` der har tomme MnT-felter — slettede policy_set_name/authz_rule_name/dacl/vlan/sgt ved enhver efterfølgende session-event. `_reconcile_from_pxgrid` havde samme problem. Fix: eksisterende cache-entry slås op og MnT-felter bevares ved merge inden upsert. **Berørte filer:** `backend/app/pxgrid/session_worker.py`.

- `[fixed 5.3.31 build 0347] 2026-05-16 — Enrichment kørte kun ved pxGrid-reconnect, ikke ved startup` — Sessioner fra disk-cache (load_from_disk) fik aldrig MnT-berigelse medmindre pxGrid genoprettede forbindelsen. Fix: periodisk enrichment-task kører ved startup (45s delay) og derefter hvert 5. min via `_mnt_enrich_loop` i `main.py`. **Berørte filer:** `backend/app/main.py`.

- `[fixed 5.3.30 build 0346] 2026-05-16 — Session-kolonne viser ikke Auth/Authz politik-navne` — `fetch_session_by_mac` kaldte kun MnT Session/MACAddress som ikke indeholder ISEPolicySetName/AuthorizationPolicyMatchedRule. Disse felter sidder i MnT **AuthStatus/MACAddress** — men probe brugte forkert URL (manglede påkrævede `/{seconds}/{records}/{framed}` path-params og fik 404). Fix: `fetch_session_by_mac` kalder nu BEGGE endpoints; AuthStatus-URL udvidet til `/3600/25/All`; `_enrich_sessions_from_mnt` populerer `policy_set_name` + `authz_rule_name` fra MnT. **Berørte filer:** `backend/app/ise/mnt_sessions.py`, `backend/app/pxgrid/session_worker.py`.

- `[fixed 5.3.30 build 0346] 2026-05-16 — ISE Profil-kolonne redundant med eksisterende Profil-kolonne` — Ny `ise_profile`-kolonne (b0345) viste `endpoint_policy` fra MnT — dublerede den eksisterende "Profil"-kolonne (vendor/profileName fra ERS). Fjernet kolonne inkl. CSS, i18n, template. **Berørte filer:** `frontend/js/views/browse-utils.js`, `frontend/js/views/browse-table.js`, `frontend/js/i18n.js`, `frontend/css/styles.css`.

- `[fixed 5.3.23 build 0331] 2026-05-15 — Platform-kolonne: ⚡-badge synker til bunden ved høj række; kan ikke sortere` — Bug 1: `display:flex` direkte på `<td>` er ugyldigt i table-layout og giver uforudsigelig vertikal alignment — indholdet faldt til bunden når en anden celle udvidede rækken. Fix: flex fjernet fra td, indhold wrappet i `<div class="platform-auto-wrap">` (flex-container). De dynamiske opdateringsveje (refreshRows/updateRow) opretter nu wrapperen on-the-fly og river den ned igen ved auto→manual-overgang. Bug 2: Sortering på platform_type brugte `r.platform_type` (ISE-gemt værdi), men auto-afledte rækker viser `nas_device_type` fra pxGrid-sessionen — de to var aldrig synkroniserede, så sorteringen viste ingen effekt. Fix: sort-logikken i `browse-filter.js` special-caser `platform_type` og kigger i `state.pxgridSessionData` (som visningen gør), med fallback til `r.platform_type`. **Berørte filer:** `frontend/css/styles.css`, `frontend/js/views/browse-table.js`, `frontend/js/views/browse-filter.js`.

- `[fixed 5.3.22 build 0327] 2026-05-15 — Profil-kolonne viser intet selvom ISE har profil-info` — `/ers/config/profilerprofile` (bulk list) fejlede gentagne gange med transport errors (timeout/forbindelsesfejl), så `profiler._cache` forblev tom og `resolve_name_sync()` returnerede altid "". **Analyse:** Log viste `ISE retry #1/#2 → ISE transport error` ved hvert forsøg. Bulk list-endpoint returnerer hundredvis af profiler og timeout'er. **Fix:** (1) Ny `resolve_name_lazy()` i `profiler.py` henter én enkelt profil direkte via `/ers/config/profilerprofile/{uuid}` (lille request, lav timeout-risiko). Cacher negativ-svar for at undgå hammer mod ISE. (2) `_fetch_endpoint_detail` bruger `resolve_name_lazy` i stedet for `ensure_loaded + sync lookup`. (3) Open API-fallback: hvis `raw.endpointProfile` (streng) er tilgængelig, bruges det direkte uden ISE-kald, og UUID→navn gemmes i cache. **Berørte filer:** `backend/app/ise/profiler.py`, `backend/app/services/endpoint_service.py`.

- `[fixed 5.3.19 build 0323] 2026-05-15 — Platform type forsvinder efter portal-genstart` — Disk-loadede sessions med `nas_device_type` mistede platform type ved reconnect. Bug 1: `_reconcile_from_pxgrid` byggede `info_with_mac` uden at kopiere `nas_name`/`nas_device_type` fra `_build_session_info()`. Bug 2: `_reconcile_from_mnt` seedede sessions uden NAS device-opslag. Fix: Begge reconcile-flows inkluderer nu korrekt NAS-opslag og falder tilbage til disk-cachet `nas_device_type` hvis NAS-cachen ikke er indlæst endnu. **Berørte filer:** `backend/app/pxgrid/session_worker.py`.

- `[fixed 5.3.11 build 0310] 2026-05-14 — Platform ⚡-badge ødelægger 2-kolonne layout i detail-modal` — `detailPtEl.after(badge)` indsatte badge-span som selvstændig CSS grid-item i `.detail-grid { grid-template-columns: 160px 1fr }`, hvilket skubbede alle efterfølgende label/input-par ud af alignment. **Fix:** `<select id="d-platformtype">` wrappet i `<div class="platform-field-wrap">` i `browse.js`; `browse-detail.js` ændret fra `detailPtEl.after(badge)` til `detailPtEl.parentElement.appendChild(badge)` så badge forbliver inden i wrapper-div'en (samme grid-celle). **Berørte filer:** `frontend/js/views/browse.js`, `frontend/js/views/browse-detail.js`, `frontend/css/styles.css`.

- `[fixed 5.3.9 build 0308] 2026-05-14 — Platform-dropdown låses ikke for endpoints med eksisterende platform_type i ISE` — `!r.platform_type`-betingelsen i `renderRows()`, `refreshRows()` og `openDetail()` forhindrede auto-lock: hvis endpoint allerede havde en gemt `platform_type` i ISE, blev `nasPt` altid tom og dropdown forblev editerbar trods aktiv pxGrid NAS-session. **Fix:** Betingelsen fjernet helt — NAS session vinder altid over gemt ISE-værdi, da Platform-feltet udelukkende bruges til portalens COA-beslutninger. **Berørte filer:** `frontend/js/views/browse-table.js`, `frontend/js/views/browse-detail.js`.

- `[fixed 5.3.2 build 0288] 2026-05-14 — TACACS+-brugere fik 401 på pxGrid SSE-stream — PULL-badge permanent` — SSE-endpointet `/api/pxgrid/sessions/stream` lavede manuel token-validering der manglede TACACS+-håndteringen. TACACS+-brugere har ingen lokal record i `users.json` → `find_by_id` returnerede None → 401 → EventSource fejlede lydløst → `pxgridLive` forblev false → badge viste 🟡 PULL selvom worker var connected og cache havde sessioner. **Fix:** tilføjet `if payload.get("auth_type") == "tacacs"` check i SSE-auth, svarende til logikken i `deps.get_current_user`. **Berørt fil:** `backend/app/api/pxgrid.py`.

- `[fixed 5.3.1 build 0286] 2026-05-14 — pxGrid session-cache starter altid på 0 — ISE replayer ikke eksisterende sessioner ved subscribe` — ISE pxGrid sender kun events for fremtidige state-ændringer. Eksisterende aktive sessioner (f.eks. 29 fra MnT) vises aldrig i pxGrid-cachen med mindre de ændrer state. Resulterede i: snapshot med 0 sessioner → badge viste PUSH 0 → alle rækker farvet rød (auth-failed) → `activeSessionMacs` overskrevet med tom Set. **Fix:** `_reconcile_cache_with_mnt()` udvides med "seed"-fase: MnT-sessioner der mangler i cachen upsert'es med `state=STARTED`. pxGrid-events opdaterer dem med policy_set_name/authz_profiles ved næste state-skift. Frontend-snapshot-handler overskriver nu kun `activeSessionMacs` når pxGrid-snapshot har sessions (>0); ellers bevares MnT-data. Badge-tæller bruger `activeSessionMacs.size` (fusioneret) i stedet for `pxgridSessionMacs.size`. **Berørte filer:** `backend/app/pxgrid/session_worker.py`, `frontend/js/views/browse.js`.

- `[fixed 5.3.0 build 0284] 2026-05-14 — Browse badge viser PULL selvom pxGrid-worker er OK` — Når pxGrid-settings gemmes, stopper backend workeren (`worker.stop()`) og genstarter den. `worker.stop()` broadcaster `pxgrid_disabled` med `reason: worker_stopped` på SSE-kanalen. Frontend-handleren kalder `stopPxGridStream()` som lukker EventSource og sætter `pxgridEventSource = null`. `startPxGridStream()` kaldes kun én gang ved view-initialisering og aldrig igen → SSE-stream er permanent død → badge viser PULL (MnT-poll) selvom workeren er genstartet og kører fint. **Fix:** `pxgrid_disabled`-handleren parser nu `reason`-feltet; er det `worker_stopped` schedules en `setTimeout(..., 5000)` der kalder `startPxGridStream()` igen. Er det `pxgrid_enabled=false` sker ingen genopretning (permanent). **Berørt fil:** `frontend/js/views/browse.js`.

- `[fixed 5.2.1 build 0275] 2026-05-14 — Portal staler intermittent — pxGrid starter aldrig pga. Windows-sti i cert-path` — Settings indeholder en Windows-absolut sti (`C:\Projekter\...\pxgrid\hypervision-portal.cert.pem`) der blev gemt fra dev-maskinen. På Linux returnerer `Path("C:\\...").is_absolute()` False, så `_resolve()` i `cert_manager.py` joiner stien til `BACKEND_ROOT/"C:\..."` — en sti der aldrig eksisterer. pxGrid-workeren fejler hvert forsøg og giver op (eksponentiel backoff, 64s interval). Uden pxGrid opdateres session-data aldrig → portal viser stale info. **Fix:** `_resolve()` detekterer Windows drive-letter-mønster (`C:\` / `C:/`) og bruger kun filnavnet resolved mod `BACKEND_ROOT/pxgrid/`. **Berørte filer:** `backend/app/pxgrid/cert_manager.py`.

- `[fixed 5.2.1 build 0272] 2026-05-14 — "Opret manglende profiler" fejler 500 på Endpoint_VLAN — EndPoints ikke ODBC dictionary` — ISE returnerer `500: EndPoints is not a valid ODBC dictionary. Only ODBC dictionaries are allowed in Common tasks.` fordi `vlan.nameID = "EndPoints:AuthzVlan"` bruger common task VLAN-feltet der kun accepterer ODBC dictionaries. **Fix:** `vlan`-feltet fjernet; erstattet med `advancedAttributes` med tre RADIUS Tunnel-attributter: `Radius:Tunnel-Type = 13`, `Radius:Tunnel-Medium-Type = 6`, `Radius:Tunnel-Private-Group-ID = EndPoints:AuthzVlan` (dynamisk). **Berørte filer:** `backend/app/services/authz_profile_service.py`.

- `[fixed 5.2.1 build 0271] 2026-05-14 — "Opret manglende profiler" fejler 500 på Endpoint_PSK-KEY — forkert dictionary` — ISE returnerer `500: Unable to find dictionary attribute for [Cisco-AV-Pair:Cisco-AV-Pair]`. Det korrekte RADIUS-dictionary i ISE for cisco AV-pairs er `Cisco` med attributten `cisco-av-pair` (lowercase), ikke `Cisco-AV-Pair:Cisco-AV-Pair`. **Berørte filer:** `backend/app/services/authz_profile_service.py`.

- `[fixed 5.2.1 build 0270] 2026-05-14 — "Opret manglende profiler" fejler 400 på Endpoint_PSK-KEY` — ISE ERS feltnavnet for højre side af advancedAttributes er `rightHandSideAttribueValue` (ISE's eget typo-mønster: "Attribue" = "Attribute" uden 't'), men koden sendte `rightHandSideAttribValue` (mangler 'ue'). Samme typo-mønster som det kendte `leftHandSideDictionaryAttribue`. **Fix:** omdøbt til `rightHandSideAttribueValue` i alle tre standard-profiler der bruger advancedAttributes. **Berørte filer:** `backend/app/services/authz_profile_service.py`.

- `[fixed 5.2.0 build 0265] 2026-05-14 — Politikker-siden viser dansk tekst selvom locale er sat til engelsk` — `nav.policy` nøglen manglede fra i18n.js, så `updateNavLabels()` i app.js sprang translationen over og bevarede hardkodet "Politikker". Hele `policy.js` og `policy-condition-builder.js` brugte hardkodede danske strings fremfor `t()`. **Berørte filer:** `frontend/js/i18n.js`, `frontend/js/views/policy.js`, `frontend/js/views/policy-condition-builder.js`, `frontend/index.html`.

- `[fixed 5.0.1 build 0248] 2026-05-13 — Deployment-opdatering fejler med PermissionError på read-only filer` — `apply_package` i `update_service.py` markerede hele opdateringen som fejlet når `CHANGELOG.md`, `FEATURES.md`, `BUGS.md`, `ARCHITECTURE.md`, `ISE_API_REFERENCE.md` og `START.bat` ikke var skrivbare for portal-processen på Linux (`/opt/hypervision/`). **Fix:** `PermissionError` fanges nu separat og tilføjes `skipped`-listen med en log-advarsel — opdateringen fortsætter og lykkes for de filer der kan skrives. **Filer:** `backend/app/services/update_service.py`.

- `[fixed 5.0.1] 2026-05-13 — Policy regel-oprettelse fejler med 400 hvis navn indeholder kolon` — ISE Open API kræver regelnavne matcher `^[\w\-\.\(\)\ ]+$` — kolon (fra MAC-adresse i standardnavn) er ikke tilladt. Desuden parsede `client.py` ikke Open API's fejlformat `{"message":"...","code":400}`, kun ERS-format. **Fix**: (1) frontend `browse-detail.js`: erstat `:` med `-` i standardnavn (`AA-BB-CC-DD-EE-FF`). (2) `backend/app/api/policy.py`: pre-validér navn mod regex og returner 400 med dansk besked. (3) `backend/app/ise/client.py`: parse begge fejlformater (ERS + Open API). **Berørte filer**: `frontend/js/views/browse-detail.js`, `backend/app/api/policy.py`, `backend/app/ise/client.py`.

- `[fixed 3.30.0 build 0213] 2026-05-08 — Browse Views: "Ryd alle filtre" nulstillede ikke filtre` — `views-clear`-handleren kaldte `applyFilterSnapshot()` med et objekt der refererede `filterFieldSel` og `filterOpSel` (fjernet i b0210) → ReferenceError → intet blev nulstillet. Derudover nulstilledes `sortCol`/`sortDir` ikke ved snapshot-apply. **Fix:** fjernet ugyldig `server`-reference fra `views-clear`-kaldet; tilføjet `sortCol`/`sortDir` reset + `updateSortHeaders()` i `applyFilterSnapshot()` så alle kald-steder (clear, apply view, restore) nulstiller sort. **Filer:** `frontend/js/views/browse-filter.js`.

- `[fixed 3.29.3 build 0204] 2026-05-08 — Browse: nye custom attributter vises ikke i dropdowns hvis filter er aktivt` — `state.caValues` populeres kun i `load()`. Når et endpoint-detail åbnes, kører `auto_discover_values` på backenden og skriver nye værdier til JSON — men frontend opdaterer aldrig `state.caValues`. Uden filter kalder et gemt endpoint `load()` (fuld reload) som tilfældigvis henter friske caValues. Med aktivt filter bruges `refreshRows()` i stedet, og `state.caValues` forbliver forældet. **Fix:** Hent `api.listCustomAttributes()` sekventielt efter `api.getEndpoint()` i `openDetail`, opdatér `state.caValues` inden dropdowns renderes. **Filer:** `frontend/js/views/browse-detail.js`.

- `[fixed 3.25.4 build 0192] 2026-05-08 — Access token udløber ikke fra brugerens perspektiv ved backend-genstart` — Token TTL er 24 h, men to fejl giver tilsammen "evig" session: (1) `boot()` i `app.js` har en catch-blok der bruger cached localStorage-bruger uden validering hvis `authStatus()`-kaldet fejler (f.eks. hvis backend er langsom at starte). (2) Der er ingen client-side exp-tjek — frontend kontrollerer aldrig om tokenet er udløbet inden det sendes. Hvis backenden startes langsomt og det første `authStatus()`-kald failer, sidder brugeren logget ind med en evt. udløbet token. **Filer:** `frontend/js/auth.js`, `frontend/js/app.js`, `backend/app/core/auth.py`.

- `[fixed 3.25.3 build 0186] 2026-05-07 — Ny skabelon vises ikke i bruger-skabelontildeling uden manuel reload` — `initUsersSection` hentede `allTemplates` én gang ved sektion-init. Oprettede admin en skabelon i Skabeloner-tabben og skiftede til Adgang-tabben uden at genindlæse siden, stod `allTemplates` stadig tom og ingen checkboxes vistes for `registrar_templet`-brugere. **Fix:** `allTemplates`-fetch flyttet ind i `reload()` så listen altid er frisk ved hvert bruger-genindlæsning. **Filer:** [frontend/js/views/settings.js](frontend/js/views/settings.js).

- `[fixed 3.25.2 build 0185] 2026-05-07 — Genstart-knap forsvandt fra Settings → Opdatering efter global .hidden-fix` — "Genstart server"-knappen lå inde i `<div id="update-result" class="hidden">` og var kun synlig fordi `.hidden` ikke havde nogen CSS-regel. Da 3.24.1-bugfikset tilføjede den globale `.hidden`-regel blev knappen korrekt skjult — men den burde altid være tilgængelig uafhængigt af om en opdatering er kørt. **Fix:** restart-knappen er flyttet til en permanent-synlig sektion under upload-formularen; restart-meddelelser vises nu i `#update-msg` frem for `#update-result-msg`. **Filer:** [frontend/js/views/settings.js](frontend/js/views/settings.js).

- `[fixed 3.24.1 build 0179] 2026-05-07 — .hidden CSS-klasse ikke defineret globalt — tpl-form-wrap altid synlig` — `settings.js` skabelon-formular brugte `class="hidden"` til at skjule formularen, men der fandtes ingen global `.hidden { display: none; }` regel i `styles.css` (kun komponent-specifikke varianter som `.ca-add.hidden`). Formularen var derfor altid synlig frem for kun ved klik på "Ny skabelon"/"Redigér". **Fix:** tilføjet `.hidden { display: none !important; }` øverst i `frontend/css/styles.css`. **Filer:** [frontend/css/styles.css](frontend/css/styles.css).

- `[fixed 3.16.0 build 0170] 2026-05-07 — Non-admin Browse henter ALLE endpoints ved role-filter (skaleringsblokker ved 10K)` — ISE ERS 3.4 understøtter ikke filter på custom attributes, så `HypervisionRoles`-scoping kræver at portalen henter **alle** endpoint-detaljer post-fetch og filtrerer i Python. Ved 10K endpoints og en non-admin med adgang til 50: portalen henter 100 sider × 100 endpoints = 10.000 details fra ISE per Browse-åbning (≈7 min). Samme problem i registrars "Mine endpoints" (`list_all_endpoint_details`). **Fix:** byg et in-memory roles-indeks i `EndpointCache` der mappes `lowercase_rolle → {endpoint_id}`; vedligeholdes ved `put_detail`/`invalidate_detail`/`invalidate_all`/`load_from_disk`. Non-admin Browse slår op i indekset (O(1)), paginerer over matches og fetcher kun de relevante details fra cache. **Filer:** [backend/app/core/endpoint_cache.py](backend/app/core/endpoint_cache.py), [backend/app/services/endpoint_service.py](backend/app/services/endpoint_service.py).

- `[fixed 3.16.0 build 0170] 2026-05-07 — save_to_disk blokerer asyncio event loop ved stort endpoint-antal` — `cache.save_to_disk()` kalder `json.dumps` på alle cached Pydantic-modeller + `path.write_text()` synkront på event loop. Ved 10K endpoints: CPU-bundet `json.dumps` tager 200–500 ms og fil-write op til 200 ms — i alt 300–700 ms hvor backend ikke kan besvare HTTP-requests. Trigger ved pre-warm scan-afslutning og ved portal-shutdown. **Fix:** `save_to_disk_async()` der kører `save_to_disk` i `asyncio.get_running_loop().run_in_executor(None, ...)`. `cache_prewarm._save_to_disk()` gøres async og awaiter den nye variant. **Filer:** [backend/app/core/endpoint_cache.py](backend/app/core/endpoint_cache.py), [backend/app/services/cache_prewarm.py](backend/app/services/cache_prewarm.py).

- `[fixed 3.16.0 build 0170] 2026-05-07 — IseEndpointGroupRepository.list_all() henter kun side 1 — fejler ved >100 ISE-grupper` — `list_all()` kalder ISE med `{"size": 100}` uden paginering. ISE ERS returnerer max 100 poster pr. side. ISE-deployments med >100 endpoint-identity-groups (ikke usædvanligt i store miljøer) returnerer kun de første 100 — resterende grupper udelades. Konsekvens: `group_name` vises som tom streng for endpoints i grupper der falder uden for side 1, og `_resolve_group_name()` misser opslag. **Fix:** paginér `list_all()` identisk med `IseEndpointRepository.list_all()`. **Filer:** [backend/app/ise/endpoints.py](backend/app/ise/endpoints.py).

- `[fixed 3.16.0 build 0170] 2026-05-07 — disk_stale_count() er O(N) scan — blokerer ved mange cache-entries` — `disk_stale_count()` itererer over hele `_details`-dict for at tælle entries med `from_disk=True`. Kaldes ved hvert cache-stats-kald (Settings → Cache, live refresh). Ved 10K cached entries: 10K-iteration per stats-kald. **Fix:** vedligehold `_disk_stale_count: int` counter inkrementelt i `put_detail`, `invalidate_detail`, `invalidate_all` og `load_from_disk`; `disk_stale_count()` returnerer counteren direkte (O(1)). **Filer:** [backend/app/core/endpoint_cache.py](backend/app/core/endpoint_cache.py).

- `[fixed 3.15.5] 2026-05-07 — 502/teknisk fejlbesked vist for admin i edit-modal ved ISE-fejl` — Alle ISE-fejl viste rå intern tekst (`ISE API 0: transport error:`) og statuskode 502 uanset årsag. Admin kan ikke bruge den information. **Fix:** ny `_ise_http_error()` helper i `api/endpoints.py` oversætter alle `IseApiError` til passende HTTP-statuskoder med dansk brugerbesked: transport-fejl → 503 "ISE er midlertidigt utilgængelig — prøv igen om lidt", uventet ISE-fejl → 502 "ISE returnerede en uventet fejl (HTTP NNN)", 404 → "Endpoint ikke fundet". Frontend edit-modal viser nu passende tekst pr. statuskode + "Prøv igen"-knap ved 503. **Filer:** [backend/app/api/endpoints.py](backend/app/api/endpoints.py), [frontend/js/views/browse.js](frontend/js/views/browse.js).

- `[fixed 3.15.4] 2026-05-07 — Portal/Logs viser gammel log fra 2026-05-05 (forkert log-sti i API)` — `logs.py` resolverede relativ log-sti med `Path.cwd()` (projektrod) → læste fra `projekt-root/logs/app.log`. Men `logging.py` skriver til `backend/logs/app.log` (relativ til `_BACKEND_DIR`). Portalen viste dermed den gamle fil der aldrig opdateres. **Fix:** `logs.py` bruger nu `Path(__file__).resolve().parents[2]` (= backend-dir) til at resolvere relativ sti — præcis samme logik som `logging.py`. **Filer:** [backend/app/api/logs.py](backend/app/api/logs.py).

- `[fixed 3.15.4] 2026-05-07 — 502 i edit-modal ved kortvarig ISE-fejl` — `force_fresh=True` rammer ISE direkte; ISE transport-fejl (timeout, reset) giver 502 til brugeren. **Fix:** `get_detail()` fanger ISE-fejl og returnerer cached entry (markeret ⏱ stale) hvis en findes, i stedet for at propagere fejlen. Kun hvis der slet ingen cached entry er returneres 502. **Filer:** [backend/app/core/endpoint_cache.py](backend/app/core/endpoint_cache.py).

- `[fixed 3.15.3] 2026-05-07 — "Task exception was never retrieved" + gruppenavn N-dobbelt ISE-kald` — (1) `_fetch_and_store()` brugte `raise` efter logging, men SWR fire-and-forget tasks blev aldrig awaited → asyncio loggede ERROR for hver ISE-timeout under pre-warm scanning. Fix: `add_done_callback` markerer exception som "retrieved" for asyncio uden at ændre adfærd for direkte awaits. Samme fix for groups-inflight. (2) `get_groups()` miss-path manglede coalescing → N samtidige `_resolve_group_name`-kald (fra N parallelle endpoint-fetches) ramte alle ISE med `list_all()` selvstændigt. Fix: `_get_or_create_groups_inflight()` + Lock i `_resolve_group_name()`. **Filer:** [backend/app/core/endpoint_cache.py](backend/app/core/endpoint_cache.py), [backend/app/services/endpoint_service.py](backend/app/services/endpoint_service.py).

- `[fixed 3.15.2] 2026-05-05 — Race condition: edit-modal langsom + dobbelt ISE-kald` — To problemer: (1) `openDetail()` kaldte `prioritizeEndpoint` (hot queue) og `getEndpoint` (force_fresh ISE-fetch) simultant → to uafhængige ISE-kald for samme endpoint. (2) `get_detail()` miss-path brugte ingen request-koalescering → concurrent requests (pre-warm worker + bruger) ramte ISE selvstændigt. Pre-warm worker med Semaphore(5) optog alle ISE-forbindelser, så brugerens fetch ventede på en ledig slot. **Fix**: `endpoint_cache.py` refaktoreret til unified `_get_or_create_inflight()` der koalescerer alle samtidige fetches for samme endpoint til ét ISE-kald; `GET /{id}` sætter altid `force_fresh=True`; `openDetail()` fjernet `prioritizeEndpoint` fire-and-forget kald. **Filer:** [backend/app/core/endpoint_cache.py](backend/app/core/endpoint_cache.py), [backend/app/api/endpoints.py](backend/app/api/endpoints.py), [frontend/js/views/browse.js](frontend/js/views/browse.js).

- `[fixed 3.13.7] 2026-05-04 — pxGrid worker reconnecte ikke efter idle timeout: backoff reset-logik fejler` — `connected_ok` i `_run_loop` sættes kun True når `_one_session()` returnerer via graceful shutdown. Ethvert exception (inkl. recv_timeout) efterlader `connected_ok=False` → backoff nulstilles aldrig → efter ~9 fejl er backoff maxet på 300s og worker venter 5min mellem hver reconnect-forsøg. **Fix (3.13.7)**: backoff-reset baseres på `last_connect_at > iter_start` (dvs. om SUBSCRIBE lykkedes denne iteration), ikke på `_one_session()` return-value. **Filer:** [backend/app/pxgrid/session_worker.py](backend/app/pxgrid/session_worker.py).

- `[fixed 3.13.6] 2026-05-04 — pxGrid worker disconnecter hvert 120s under idle: "Ingen frames i 120s"` — ISE pxGrid broker sender ingen STOMP-frames (hverken MESSAGE eller heartbeat) under idle-perioder der kan overstige 120s. `recv_timeout = 120.0` (hardkodet) i session_worker.py trigger timeout selvom WebSocket-forbindelsen er fuldt intakt (ping/pong kører hvert 20s). Worker genstartede 33+ gange og session-cache nulstilles ved reconnect. **Root cause**: recv_timeout for aggressiv — ISE broker er passiv men levende. **Fix (3.13.6)**: `recv_timeout` læses fra ny config-setting `pxgrid_stomp_recv_timeout_s` (default 600s). WebSocket ping/pong (ping_interval=20, ping_timeout=10) detekterer dead TCP inden for 30s uafhængigt. **Filer:** [backend/app/pxgrid/session_worker.py](backend/app/pxgrid/session_worker.py), [backend/app/core/config.py](backend/app/core/config.py).

- `[fixed 3.12.0] 2026-05-03 — editor-psk kan ikke redigere tildelte System adm-roller` — `canEditRoles` i browse.js og `canPickRoles` i register.js ekskluderer `editor-psk`-rollen, så chips er read-only for psk-editors. **Berørte filer:** `frontend/js/views/browse.js`, `frontend/js/views/register.js`.

- `[fixed 3.12.4] 2026-05-03 — Rediger valgte endpoints mangler Tilknytning, PSK Mode og PSK Key` — Bulk-edit modal har ikke de samme felter som Endpoint detaljer detail-modal (mangler Tilknytning/static group, PSK Mode, PSK Key). **Berørte filer:** `frontend/js/views/browse.js`.

- `[fixed 3.12.5] 2026-05-03 — Bulk-edit modal ændrer størrelse ved Vis/PSK Key-aktivering` — `.modal` har kun `width: 420px` men PSK Key-sektionen kræver 560px (som detail-modal). Bulk-edit modal-div manglede `detail-modal` klassen. **Berørte filer:** `frontend/js/views/browse.js`.

## Fixed

- `[fixed 3.10.1] 2026-05-03 — Tab-skift til Browse/Edit: alle endpoints rød til manuel Refresh` — Ved view-mount kaldte den initielle `load()` `refreshActiveSessionMacs(false)`. SSE-snapshot-event kan ankomme under load med `sessions=[]` (tom backend-cache) og sætter `activeSessionMacs = new Set([])` (tom Set er truthy!). `refreshActiveSessionMacs(false)` ser `pxgridLive && pxgridSessionMacs` (tom Set ≠ null) og returnerer med den tomme Set — MnT polles aldrig — alle endpoints forbliver røde. **Fix (3.10.1)**: Det initielle `load()` på view-mount ændret til `load(true)` — poller altid MnT ved view-init uanset pxGrid-snapshot-state. **Filer:** [frontend/js/views/browse.js](frontend/js/views/browse.js).

- `[fixed 3.10.0] 2026-05-03 — Refresh poller ikke MnT når pxGrid er aktiv — alle devices forbliver grønne` — `refreshActiveSessionMacs(force)` havde pxGrid early-return på linje 1 **før** `force`-tjekket — når `pxgridLive && pxgridSessionMacs` var sandt returnerede funktionen altid med pxGrid-data, og `force=true` fra Refresh-knappen blev aldrig evalueret. pxGrids `pxgridSessionMacs` er et inkrementelt view bygget fra SSE-events, ikke et autoritativt snapshot — ved portal-reload eller stale cache kan det indeholde MACs for endpoints der er disconnected. MnT's `ActiveList` er den fulde sandhed. **Fix (3.10.0)**: `force`-tjekket wrapper nu begge early-returns — ved `force=true` bypasses al pxGrid-logik og MnT polles altid. Efter MnT-poll synkroniseres `pxgridSessionMacs` med det autoritative MnT-set så fremtidige pxGrid-events bygger på korrekt fundament. **Filer:** [frontend/js/views/browse.js](frontend/js/views/browse.js).

- `[fixed 3.9.8] 2026-05-03 — Refresh i Browse/Edit henter ikke aktuel ISE-status: cache bruges + auth-farver mangler uden filter` — Refresh-knappen kaldte `load()` direkte uden cache-bust, så backend serverede cached endpoint-data i stedet for live ISE-data. Derudover: `refreshActiveSessionMacs()` sprang MnT-poll over når `!anyFilterActive()` — endpoint-rækker viste ingen grøn/rød auth-status ved Refresh uden aktivt filter (f.eks. efter portal-genstart). **Fix (3.9.8)**: (A) `POST /cache/invalidate` ændret fra `require_admin` til `require_any` så alle brugere kan trigge cache-bust. (B) Refresh-knap kalder `invalidateCache()` + `load(true)`. (C) `load(force)` og `refreshActiveSessionMacs(force)` tager `force`-parameter — ved `force=true` poller vi altid MnT uanset filter-state. Refresh viser "Opdaterer…" under operationen. **Filer:** [frontend/js/views/browse.js](frontend/js/views/browse.js), [backend/app/api/cache.py](backend/app/api/cache.py).

- `[fixed 3.9.7] 2026-05-03 — Audit-log aktør viser altid "system" i stedet for den indloggede bruger` — `audit_store.record()` læser aktøren fra `actor_ctx` (en `ContextVar`). `actor_ctx.set()` kaldes korrekt i `get_current_user()` i `deps.py`, men `get_current_user` var en sync `def`-funktion — FastAPI kører sync dependencies via `run_in_executor` (threadpool), og `ContextVar.set()` i en thread modificerer kun threadens kontekst-kopi. Ændringen propagerer ikke tilbage til den asyncio-task hvor `audit_store.record()` kører, så `actor_ctx.get()` altid returnerer default `ActorContext(actor_username="system")`. **Fix (3.9.7)**: `get_current_user` ændret fra `def` til `async def` — FastAPI awaiter den nu direkte i den aktuelle asyncio-task, og `actor_ctx.set()` er synlig for alle efterfølgende `record()`-kald i samme request. **Filer:** [backend/app/api/deps.py](backend/app/api/deps.py).

- `[fixed 3.9.6] 2026-05-03 — Ny bruger tildeles ikke automatisk sin egen System adm-rolle ved oprettelse` — `create_user` opretter korrekt en System adm-rolle i kataloget med navn = username (3.8.0), men initialiserer `assigned_endpoint_roles = []`. UI-tabel viser dermed rollen som unchecked selv om den eksisterer i kataloget — admin skal manuelt gå ind og tilvælge den. `effective_roles()` tilføjer `username` implicit (så adgangen ER der teknisk), men det er usynligt i UI og forvirrende. **Fix (3.9.6)**: `create_user` sætter `assigned_endpoint_roles = [username]` når `ensure_user_role` lykkes. `effective_roles()` deduplicerer med `dict.fromkeys` så `["jan", "jan"]` ikke kan opstå for nye brugere. **Filer:** [backend/app/services/user_service.py](backend/app/services/user_service.py).

- `[fixed 3.9.5] 2026-05-03 — pxGrid worker oscillerer: ISE sender ingen STOMP-heartbeats → worker timeout hvert 60s` — ISE's pxGrid STOMP-broker på `ise2.ll.lan` sender ingen heartbeat-frames, hverken ved `heart-beat: 0,30000` eller `heart-beat: 0,0`. Worker'en venter `2 × heartbeat_ms = 60s`, timeout trigges, connection lukkes, worker genstarter. Bevises i `logs/app.log`: *"Ingen frames i 60s (heartbeat-tab)"* præcist hvert 60. sekund kontinuerligt. SSE-badge oscillerer ⚪↔🟢 pga. EventSource auto-reconnect (~3s browser default). **Root cause**: ISE pxGrid-broker ignorerer heartbeat-anmodningen; portalen har ikke alternativ liveness-mekanisme. **Fix (3.9.5)**: brug WebSocket RFC 6455 ping/pong (`ping_interval=20s, ping_timeout=10s`) som primær liveness — uafhængigt af STOMP-heartbeats. `recv_timeout` sættes til 120s som backstop. **Filer:** [backend/app/pxgrid/session_worker.py](backend/app/pxgrid/session_worker.py).

- `[fixed 3.9.5] 2026-05-03 — SSE badge-flicker ⚪↔🟢 hvert ~3s ved transient SSE-reconnect` — Browser's `EventSource.onerror` sætter øjeblikkeligt `pxgridLive=false` → badge → ⚪. Default browser reconnect er ~3s → `onopen` → `pxgridLive=true` → badge → 🟢. Gentages synligt hvert 3. sekund. **Fix (3.9.5)**: debounce `onerror` med 5s grace period; hvis `onopen` ankommer inden for 5s afbrydes timer og badge forbliver 🟢. **Filer:** [frontend/js/views/browse.js](frontend/js/views/browse.js).

- `[fixed 3.9.5] 2026-05-03 — Disconnected endpoint forbliver grøn (auth-active) i Browse/edit når pxGrid kører` — To root causes: (A) `applyAuthStatusColors()` har early-return når `activeSessionMacs === null`; dette sker fx. når `refreshFilters` kaldes uden aktivt filter og sætter `activeSessionMacs = null` — evt. `remove`-event kan derefter ikke farve rækken rød. (B) Når worker genstarter efter 60s-timeout misses disconnect-events der skete under genopbygningsvinduet: `cache.remove(mac)` returnerer `existed=False` → ingen `remove`-broadcast → frontend opdateres aldrig. **Fix (3.9.5)**: (A) `applyAuthStatusColors` falder tilbage til `pxgridSessionMacs` når pxGrid er live og `activeSessionMacs` er null. (B) Worker reconcilerer session-cache mod MnT `ActiveList` efter hver reconnect. **Filer:** [backend/app/pxgrid/session_worker.py](backend/app/pxgrid/session_worker.py), [frontend/js/views/browse.js](frontend/js/views/browse.js).

- `[fixed 3.9.1] 2026-05-01 — Saved views gendanner ikke kolonne-synlighed eller page-size` — 3.9.0's `snapshotFilters()` fangede kun aktive filtre (Kun portal, server-MAC, kolonnefiltre), ikke kolonne-synlighed (Kolonner ▾) eller page-size. Det betød at hvis admin havde skjult fx Owner-kolonnen i et "Mine printere"-view og senere genvalgte det, viste alle kolonner igen. **Fix**: `snapshotFilters()` inkluderer nu `colVis` (kolonne-synlighed) + `pageSize`, og `applyFilterSnapshot()` anvender begge ved view-aktivering. Gamle views gemt før 3.9.1 mangler felterne men bevarer current state — ingen breaking change. **Filer:** [frontend/js/views/browse.js](frontend/js/views/browse.js).

- `[fixed 3.8.2] 2026-05-01 — System adm-katalog opdateres ikke i UI efter ny bruger oprettes; admin-bruger har stadig System adm-picker` — To relaterede UX-bugs efter 3.8.0 (auto-rolle pr. bruger): (a) backend opretter automatisk en System adm-rolle med navn = username ved bruger-creation, men frontend's lokale rolle-katalog blev ikke refreshed → den nye rolle var ikke synlig i Brugere-tabellens checkbox-picker indtil page reload. (b) Admin-brugere viste en aktiv System adm checkbox-picker selv om admin implicit har alle roller (ser alt) — det var forvirrende. **Fix**: (a) `initRolesSection` returnerer nu sin `reload`-funktion, og `createUser`-handler kalder den efter success så katalog-state opdateres øjeblikkeligt. (b) Brugere med `role === "admin"` viser nu i stedet hinten *"Admin — alle System adm implicit"* i System adm-kolonnen. Når admin ændrer en brugers system-rolle (fx editor → admin), genrenderes rækken så cellen skifter fra picker til hint. **Filer:** [frontend/js/views/settings.js](frontend/js/views/settings.js).

- `[fixed 3.8.1] 2026-04-30 — Registrar får 403 på "Mine endpoints" i register-viewet` — Bruger med rollen `registrar` så fejlen *"Kunne ikke hente: 403: Kræver en af rollerne: admin, editor, viewer"* når de klikkede "Mine endpoints"-knappen. Årsag: `GET /api/endpoints/details/all` brugte `require_any` (admin/editor/viewer) — registrar var udelukket selv om service-laget allerede filtrerer på effektive roller. Fix: skift til `require_register_lookup` (admin/editor/viewer/registrar). Service-laget's role-filter sikrer at registrar kun ser egne endpoints — ingen privilegie-eskalering. **Filer:** [backend/app/api/endpoints.py](backend/app/api/endpoints.py).

- `[fixed 3.7.3] 2026-04-30 — Browse-badge viser fortsat "PUSH (pxGrid)" når pxGrid er disabled, auth-status opdateres aldrig` — Når admin slog pxGrid fra i Settings, blev SSE-forbindelsen stående åben (cache-subscribe lytter uafhængigt af worker-state). Frontend så ingen onerror, så `pxgridLive` blev ved at være `true` → badge viste "🟢 PUSH (0 aktive · sidste event Xm siden)" forevigt. Konsekvens: `refreshActiveSessionMacs()` sprang MnT-poll over (fordi den tror PUSH er aktiv) → ingen auth-status-farver opdateredes. **Fix**: (a) SSE-endpoint tjekker `pxgrid_enabled` ved connect og sender `pxgrid_disabled`-event hvis disabled (returnerer derefter); (b) `worker.stop()` broadcaster også `pxgrid_disabled` så aktive SSE-clients får besked når admin disabler under drift; (c) frontend lytter på `pxgrid_disabled`, lukker EventSource permanent og falder tilbage til MnT-poll. Browse-badge viser nu korrekt "🟡 PULL (MnT-poll)" når pxGrid er disabled. **Filer:** [backend/app/api/pxgrid.py](backend/app/api/pxgrid.py), [backend/app/pxgrid/session_worker.py](backend/app/pxgrid/session_worker.py), [frontend/js/views/browse.js](frontend/js/views/browse.js).

- `[fixed 3.6.1] 2026-04-29 — Phase 4: nye endpoints i ISE dukker ikke op i portal selv om endpoint-topic er aktiv` — Worker SUBSCRIBE'ede til den hardcoded default-topic `/topic/com.cisco.ise.endpoint`, men ISE-versioner publicerer endpoint-events under forskellige service-navne (com.cisco.ise.endpoint, com.cisco.ise.config.profiler, com.cisco.ise.endpoint.asset). Hvis topic'et ikke findes accepteres SUBSCRIBE silently uden at events nogensinde kommer. Fix: worker laver nu ServiceLookup på et konfigurerbart service-navn (`pxgrid_endpoint_service`, default `com.cisco.ise.endpoint`) og bruger den returnerede `topic`-property hvis tilstede; falder tilbage til konfigureret topic ellers. ServiceLookup-fejl logges som `last_error` i worker-status så admin kan se det i Settings. UI har nu separate felter for service-navn og fallback-topic. **Filer:** [backend/app/core/config.py](backend/app/core/config.py), [backend/app/pxgrid/session_worker.py](backend/app/pxgrid/session_worker.py), [backend/app/schemas/settings.py](backend/app/schemas/settings.py), [backend/app/services/settings_service.py](backend/app/services/settings_service.py), [frontend/js/views/settings.js](frontend/js/views/settings.js).

- `[fixed 3.5.2] 2026-04-29 — SSE-stream returnerer 404, frontend falder tilbage til MnT-pull` — Browse-toolbar viste konsekvent "🟡 PULL (MnT-poll)" selv når worker'en var connected. Årsag: route `/sessions/stream` blev registreret EFTER `/sessions/{mac}` i [api/pxgrid.py](backend/app/api/pxgrid.py), så FastAPI matchede stream-URL'en som `mac="stream"`, kaldte `cache.get("stream")` → 404 → EventSource onerror → pxgridLive=false. Fix: flyttede stream-route op FØR den dynamiske `{mac}`-route og tilføjede explicit kommentar om kravet. **Filer:** [backend/app/api/pxgrid.py](backend/app/api/pxgrid.py).

- `[fixed 3.5.1] 2026-04-29 — Browse: auth-status farver opdateres ikke live, kun ved Refresh` — SSE-event handlers for `upsert/remove` opdaterede kun `activeSessionMacs` hvis et filter var aktivt; uden filter blev DOM-rækker aldrig re-coloured når events kom ind. Fix: når pxGrid stream er live populeres `activeSessionMacs` altid fra cache (SSE-data koster ikke ekstra ISE-kald), og `applyAuthStatusColors()` kaldes på hver event uafhængigt af filter. **Filer:** [frontend/js/views/browse.js](frontend/js/views/browse.js).

- `[fixed] 2026-04-28 — PxGrid: WebSocket-upgrade afvist med HTTP 401 fordi Basic-auth-header mangler på selve handshaken` — STOMP-prober nåede frem til `ws_connect`-trinet og fejlede med `server rejected WebSocket connection: HTTP 401`. **Rodårsag**: pxGrid pubsub-broker kræver HTTP Basic auth (`node_name:secret`) på selve WebSocket-upgrade-requesten — ikke kun inde i STOMP CONNECT-frame'en. mTLS alene er ikke nok til broker-laget; ISE forventer to-lags auth (mTLS-cert som identitet + Basic auth som autorisation). probe.py kaldte `websockets.connect()` uden `additional_headers`, så upgraden blev afvist *inden* vi nåede STOMP-laget. **Løsning** (build 0105): `probe.run_session_probe()` bygger nu `Authorization: Basic <b64(node:secret)>` og sender den via `additional_headers=` til `websockets.connect()`. Verificeret at `additional_headers` er det korrekte parameter-navn i websockets 16.0 (det hed tidligere `extra_headers` i ældre versioner). Filer: [backend/app/pxgrid/probe.py](backend/app/pxgrid/probe.py).
- `[fixed] 2026-04-27 — PxGrid: AccessSecret-endpoint kaldt med forkert path "/AccessSecretCreate" → ISE 3.4 returnerer 404` — STOMP-prober (build 0103) fejlede med `STOMP fejlede ved [access_secret]: AccessSecretCreate(~ise-pubsub-ise2) fejlede: PxGrid /AccessSecretCreate returned 404` selvom AccountCreate, AccountActivate og ServiceLookup alle virkede mod samme node. **Rodårsag**: pxGrid 2.0-spec'et og Cisco DevNet samples (cisco-pxgrid/pxgrid-rest-ws) bruger kortformen `/pxgrid/control/AccessSecret` her — modsat de tre andre control-plane calls der har "Create"/"Activate"/"Lookup"-suffix. Naming-mønstret bryder med de øvrige, og portalens kode antog konsistent suffix. **Løsning** (build 0104): `client.access_secret_create()` i [backend/app/pxgrid/client.py](backend/app/pxgrid/client.py) kalder nu `/AccessSecret` (ikke `/AccessSecretCreate`); fejlmeddelelser og docstrings opdateret tilsvarende. Også [ISE_API_REFERENCE.md](ISE_API_REFERENCE.md) udvidet med ny gotcha #8 så fælden ikke gentages. Filer: [backend/app/pxgrid/client.py](backend/app/pxgrid/client.py), [backend/app/pxgrid/probe.py](backend/app/pxgrid/probe.py), [ISE_API_REFERENCE.md](ISE_API_REFERENCE.md).
- `[fixed] 2026-04-27 — PxGrid: CSR har kun nodeName som SAN — host-FQDN bør med for fuld pxGrid 2.0 / RFC 6125 compliance` — Build 0101 tilføjede `SAN:dNSName=<node_name>` til CSR'en (minimumskravet for ISE 3.4-acceptance), men pxGrid 2.0 best practice + RFC 6125 §6.4 anbefaler at SAN også indeholder portalens host-FQDN. Visse ISE-deployments med stricter cert-validation (typisk efter 3.x-patches) afviser klient-certs uden FQDN, og uden FQDN kan cert'et ikke genbruges som transport-identitet i fremtidige pxGrid-features. **Løsning** (build 0102): nyt setting-felt `pxgrid_cert_extra_sans` (komma-separeret liste af DNS-navne) der inkluderes som ekstra `SAN:dNSName` i CSR'en udover `pxgrid_node_name`. Listen dedupes og tomme entries filtreres væk. UI-felt tilføjet under node-navnet i Settings → PxGrid med placeholder + hint om at tilføje host-FQDN. Påvirker kun nye CSR'er — eksisterende cert skal genskabes via Nulstil registrering → Trin 1 hvis admin vil have FQDN ind. Filer: [backend/app/core/config.py](backend/app/core/config.py), [backend/app/schemas/settings.py](backend/app/schemas/settings.py), [backend/app/services/settings_service.py](backend/app/services/settings_service.py), [backend/app/pxgrid/cert_manager.py](backend/app/pxgrid/cert_manager.py), [backend/app/api/settings.py](backend/app/api/settings.py), [frontend/js/views/settings.js](frontend/js/views/settings.js).
- `[fixed] 2026-04-27 — PxGrid: portal-genereret CSR mangler SubjectAlternativeName så ISE afviser klient-cert` — `cert_manager.generate_csr()` byggede CSR'en med kun `subject_name = CN=<node_name>` og ingen SAN-extension. ISE 3.4's pxGrid-modul valider klient-certifikater efter pxGrid 2.0 spec'et og RFC 6125 — som siden 2017 har deprecated CN-only matching og kræver `SubjectAlternativeName:dNSName` matchende nodeName. Resultat: TLS-handshake selv lykkedes (cert er signeret af en trusted CA), men pxGrid-laget afviste kontoen som "ikke matcher node", hvilket bl.a. kunne ses i ISE pxGrid-server.log som "Password mismatch" på `hypervision-portal` selvom `Web Clients`-tabben var tom — kontoen blev afvist *før* den nåede frem til client-registreret tilstand. **Løsning** (build 0101): `generate_csr()` tilføjer nu `x509.SubjectAlternativeName([x509.DNSName(common_name)])` som non-critical extension, så cert'et matcher pxGrid 2.0-spec'et. Kræver re-kørsel af CSR-flowet (Nulstil registrering → Trin 1 → ny CSR til CA → Upload signeret cert) for eksisterende installationer da SAN bages ind i certet ved CA-signering, ikke ved CSR-generering. Filer: [backend/app/pxgrid/cert_manager.py](backend/app/pxgrid/cert_manager.py).
- `[fixed] 2026-04-27 — PxGrid: "must both be set"-fejlmeddelelse er for kryptisk når et cert-felt mangler` — Efter en reset (eller en delvist gennemført upload-flow) gav AccountCreate / Test forbindelse en generisk besked: `pxgrid_cert_path and pxgrid_key_path must both be set` — uden at angive hvilken af de to der manglede eller hvad admin skulle gøre. **Løsning** (build 0100): `cert_manager.load_bundle` lister nu eksplicit hvilke felter der mangler ("klient-cert", "private key" osv.) og peger på det korrekte trin i CSR-flowet (Trin 1 for key, Trin 3 for cert, Trin 4 for CA-bundle). Også fil-eksistens-fejl peger nu admin på det rette trin at gen-køre. Filer: [backend/app/pxgrid/cert_manager.py](backend/app/pxgrid/cert_manager.py).
- `[fixed] 2026-04-27 — PxGrid: Trin 5 fejler med 503 selvom kontoen allerede er oprettet og password er gemt` — Ved en allerede vellykket AccountCreate gemmer portalen det ISE-genererede `pxgrid_password`, men efterfølgende kald til `POST /api/settings/pxgrid/account` returnerer fortsat 503 fordi ISE 3.4's pxGrid afviser at re-registrere et eksisterende `nodeName` (i stedet for den idempotente success som pxGrid 2.0-spec'et foreskriver). Resultat: admin har faktisk passeret Trin 5, men UI'et viser fejl og leder admin til at tro at intet virker. **Løsning** (build 0098): `pxgrid_account_create()` i [backend/app/services/settings_service.py](backend/app/services/settings_service.py) detekterer nu mønsteret "503 + gemt password" og falder automatisk tilbage på `AccountActivate` for at rapportere den faktiske state. Hvis activate giver ENABLED → success, hvis PENDING → success med besked om at admin skal approve i ISE, hvis activate også fejler → original 503-besked. Trin 5 er nu idempotent set fra portal-siden.
- `[fixed] 2026-04-27 — PxGrid: AccountCreate "returned 503:" uden brugbar fejlmeddelelse` — Efter PEM-validation blev fikset (build 0096) returnerede ISE pxGrid HTTP 503 med tom body på `/AccountCreate`. Portalen reportede bare "PxGrid /AccountCreate returned 503: " hvilket ikke gav admin nogen ide om årsagen. **Rodårsag (eksternt)**: 503 fra port 8910 betyder ikke at ISE er nede — det betyder at pxGrid-service-laget afviser kaldet. De realistiske årsager er (1) pxGrid-persona ikke aktiveret på den ramte node, (2) pxGrid-servicen er restartende efter config-ændring, (3) auto-approval / AccountCreate disabled, eller (4) `pxgrid_psn_fqdn` er tom så portalen rammer en non-pxGrid PSN via fallback fra `ise_base_url`. **Løsning** (build 0097): `_post` i [backend/app/pxgrid/client.py](backend/app/pxgrid/client.py) håndterer nu 503 specifikt med en dansk fejlmeddelelse der lister de tre konkrete tjekpunkter i ISE-UI (Deployment / All Clients / Settings) plus en note om at flere PSN-noder kræver eksplicit `pxgrid_psn_fqdn`. 401/403 fik også en bedre besked der peger på MS CA-trust + node-name-mismatch som de typiske årsager. Det er ikke en kode-bug i portalen — fejlmeddelelsen var bare ubrugelig.
- `[fixed] 2026-04-26 — PxGrid: AccountCreate fejler med "[SSL] PEM lib (_ssl.c:4143)" pga. CSR uploadet som signeret cert` — Trin 5 i CSR-flowet svarede `AccountCreate fejlede: [SSL] PEM lib (_ssl.c:4143)`. Inspektion af `backend/pxgrid/<node>.cert.pem` viste at filen indeholdt `-----BEGIN CERTIFICATE REQUEST-----` i stedet for `-----BEGIN CERTIFICATE-----` — admin havde uploadet selve CSR-filen som "signeret cert". **Rodårsag**: `POST /pxgrid/cert kind=cert` validerede kun `b"-----BEGIN" in raw`, hvilket en CSR også opfylder. Filen blev gemt på disken, og OpenSSL fejlede først ved TLS-handshake i httpx med en ubrugelig generisk PEM-fejl uden filnavn eller årsag. Samme klasse af problem ramte (a) PKCS#7 .p7b chains (typisk MS certsrv "Download certificate chain") som har `-----BEGIN PKCS7-----` og passerer naive check men ikke kan loades af OpenSSL `cert=` / `verify=`, og (b) DER-binær cert/key uden PEM-header. **Løsning** (build 0096): ny `cert_manager.normalize_uploaded_bytes(kind, raw)` parser indholdet via `cryptography` per kind: **cert/CA** afviser CSR med eksplicit dansk besked, accepterer X.509 PEM, konverterer PKCS#7 PEM/DER → X.509 PEM, og falder tilbage til DER X.509; **key** parses som PEM/DER private key og re-emittes som PKCS#8 PEM (password-beskyttede peges hen på PKCS#12-importen). UTF-8 BOM strippes. Upload-endpoint returnerer nu 400 med konkret dansk fejlmeddelelse i stedet for at lade fejlen ramme TLS-laget. Filer: [backend/app/pxgrid/cert_manager.py](backend/app/pxgrid/cert_manager.py), [backend/app/api/settings.py](backend/app/api/settings.py).
- `[fixed] 2026-04-26 — PxGrid: CSR-flow mangler synlig "Trin 2" + ingen feedback efter file upload` — To UX-issues opdaget under CSR-flowet: (a) Trin-nummereringen sprang fra Trin 1 → Trin 3 → Trin 3b → Trin 4 fordi "indsend CSR til CA" kun lå i hjælpeteksten øverst og ikke som synligt felt — admin opfattede det som om der manglede et trin; (b) efter upload af cert/key/CA-fil resettede `<input type=file>` til "no file chosen" (by design så samme fil kan vælges igen ved fejl), men den eneste bekræftelse var success-meddelelsen øverst i kortet og en path-opdatering langt nede i formularen — admin var ikke sikker på om upload'en faktisk virkede. **Løsning** (build 0095): (a) CSR-blokken renummereret til 5 trin (1: Generér CSR · 2: Indsend til CA + hent cert/chain — nu med eksplicit instruktion-felt med både ISE-internal-CA og MS-certsrv-flow · 3: Upload klient-cert · 4: Upload CA-bundle · 5: Opret konto), (b) hver fil-upload-felt har fået en `.upload-status` span lige under input'et der viser "Uploader filnavn..." → "✓ Uploadet: filnavn" (grøn) eller "✗ Fejl: ..." (rød), så admin har direkte visuel bekræftelse hvor de klikkede uden at skulle scanne resten af formularen. Filer: [frontend/js/views/settings.js](frontend/js/views/settings.js).
- `[fixed] 2026-04-26 — PxGrid: upload-blokken vises i CSR-mode med misvisende "Upload-mode:" headline + footgun-key-felt` — Når admin valgte CSR-mode forblev hele upload-blokken synlig (cert + key + CA + PFX) ved siden af CSR-flowet. To problemer: (a) headline-teksten "Upload-mode: upload tre PEM-filer..." gav indtryk af at admin *også* skulle gennem den blok, hvilket var forvirrende; (b) "Privat key (PEM)"-feltet var et reelt footgun — hvis admin uploadede en key dér ville det overskrive den private key som portalen lige havde genereret som del af CSR'en, så cert/key-paret matchede ikke længere. **Løsning** (build 0094): `applyMode("csr")` skjuler nu hele upload-blokken; CA-bundle-uploaden (som CSR ikke selv genererer — den bruges til at verificere ISE pxGrid-server-cert ved mTLS) er flyttet ind i CSR-blokken som "Trin 3b — Upload CA-bundle". Resultatet er ét sammenhængende CSR-flow uden synlige felter der ikke giver mening i den valgte mode. Filer: [frontend/js/views/settings.js](frontend/js/views/settings.js).
- `[fixed] 2026-04-26 — PxGrid: ingen tydelig sti til upload af signeret cert efter CSR-download` — Efter "Generér CSR" + download fra portalen kunne admin ikke se hvordan det signerede cert (returneret fra ISE internal CA / MS certsrv) skulle uploades tilbage. Hjælpe-teksten henviste til "Klient-certifikat (PEM)"-feltet i upload-blokken ovenover, men feltet lå visuelt adskilt fra CSR-flowet og blev overset. **Løsning** (build 0093): CSR-blokken er omstruktureret til 4 nummererede trin (Generér → Indsend → Upload signeret cert → Opret konto) med en dedikeret upload-felt for det signerede cert *inde i* CSR-blokken. Backend-endpoint er det samme (`POST /pxgrid/cert kind=cert`) — kun UI-flowet er ændret. Filer: [frontend/js/views/settings.js](frontend/js/views/settings.js).
- `[fixed] 2026-04-26 — PxGrid: POST /api/settings/pxgrid/csr returnerer 400 efter mode-skift i UI` — Når brugeren skiftede cert-mode fra "upload" til "csr" i Settings og klikkede "Generér CSR" *uden* først at klikke "Gem PxGrid settings", svarede backend `400 Bad Request` med "kun gyldig i 'csr'-mode". Samme gate gjaldt `POST /pxgrid/account`. **Rodårsag**: backend gatekeeper på `pxgrid_cert_mode == "csr"` (persisted state), men dropdown-værdien lever kun i DOM indtil formularen submittes — backend ser stadig `cert_mode=upload` indtil settings gemmes. **Løsning** (build 0090): (a) Backend [backend/app/api/settings.py](backend/app/api/settings.py) `/pxgrid/csr` og [backend/app/services/settings_service.py](backend/app/services/settings_service.py) `pxgrid_account_create()` validerer nu kun `pxgrid_node_name` (ikke `cert_mode`) — CSR-generering og AccountCreate er ikke-destruktive operationer som admin kan køre i hvilken som helst mode (f.eks. for at skifte over senere). (b) Frontend [frontend/js/views/settings.js](frontend/js/views/settings.js) auto-gemmer formularen (uden at røre password-feltet) før både CSR- og account-knapperne kalder backend, så persisted state matcher UI'et og password ikke wipes utilsigtet.
- `[fixed] 2026-04-26 — Audit: "Vis"/"Rollback" på hver sin linje + søgefelt fremstod som om det ikke filtrerede` — Actions-cellen i Audit-tabellen havde kolonnebredde 9rem, og standard-button-padding gjorde at "Vis" + "Rollback" ikke kunne være på samme linje — Rollback wrappede ned, og der stod et stort tomt felt til højre i rækker uden Rollback. Samtidig reagerede `#audit-search` kun på `input`-event med 350ms debounce, så hvis browseren cachede en pre-b0087 version af `audit.js` (uden search-feltet wired op til backend), så det ud som om søgningen slet ikke filtrerede. **Verificeret**: backend filtrerer korrekt (`audit_store.query(search='admin')` → 3 af 49 events). **Løsning** (build 0088): (a) kolonnen udvidet til 11rem via ny `.audit-actions-col` klasse, ny `.audit-actions-cell` med `white-space: nowrap` + højrejusteret + kompakte button-styles, så begge knapper er på samme linje uanset om Rollback faktisk vises; (b) tilføjet `change`- og `keydown`/Enter-handlers på søgefeltet så `load()` kaldes straks (uden debounce) ved blur eller Enter — hjælper både hvis bruger copy-paster og som workaround mod evt. cached gammelt JS. Filer: [frontend/js/views/audit.js](frontend/js/views/audit.js), [frontend/css/styles.css](frontend/css/styles.css).
- `[fixed] 2026-04-25 — Browse/Edit: "roleCatalog.map is not a function" på første load` — Phase 6b/6c kaldte `api.listEndpointRoles()` og brugte responsen direkte som array. Backend returnerer dog `EndpointRoleListResponse` der serialiserer til `{"roles": [...]}` (jf. [backend/app/api/endpoint_roles.py](backend/app/api/endpoint_roles.py) og [backend/app/schemas/endpoint_role.py](backend/app/schemas/endpoint_role.py)). I [frontend/js/views/settings.js](frontend/js/views/settings.js) blev der korrekt læst `data.roles`, men i [frontend/js/views/browse.js](frontend/js/views/browse.js) og [frontend/js/views/register.js](frontend/js/views/register.js) blev hele response-objektet behandlet som en array — derfor crashede `roleCatalog.map(...)` ved render af chip-kolonnen og chip-pickeren. **Løsning** (build 0086): begge filer udtrækker nu `rolesResp.roles` med `Array.isArray`-guard og fallback til `[]`. `.catch()` returnerer `{roles: []}` så samme dest-form bruges også ved fejl.
- `[fixed] 2026-04-25 — Browse/Edit: kolonner forskudt én plads til venstre fordi Vendor-kolonnen mangler i body` — Efter 2.11.0 (OUI vendor-enrichment) blev "Vendor" tilføjet til `COLUMNS`-arrayet i [frontend/js/views/browse.js](frontend/js/views/browse.js) — som genererer både header-row og filter-row — men `renderRows` blev ikke opdateret til at emittere en `<td>` for vendor. Resultat: header havde 11 kolonner (efter checkbox), body havde 10, så alt fra Identity Group og frem stod under den forkerte header (Identity Group viste vendor-værdier, Description viste Identity Group osv.). **Løsning** (build 0073): tilføjet `<td class="vendor-cell-td">${esc(r.vendor || "")}</td>` lige efter MAC-cellen i `renderRows`. Vendor er read-only (udledes fra OUI), så ingen edit-handlers kræves.
- `[fixed] 2026-04-25 — Rollback rydder custom attributes i stedet for at restore dem` — Audit-rollback af et endpoint-update satte alle custom attributes (Type, Owner, Lokation, AuthzVlan, AuthzACL, PlatformType) til tom-streng i stedet for at restore før-værdierne. F.eks. AuthzVlan ændret 64 → 100 og rollback gav `""` i stedet for `64`. **Rodårsag**: `_endpoint_update_from_snapshot` i [backend/app/api/audit.py](backend/app/api/audit.py) læste `snap.get("custom_attributes")` og `snap.get("static_group_assignment")`, men before-snapshot'et er `EndpointDetail.model_dump()` som *flader* custom attributes ud til top-level felter (`endpoint_type`, `owner`, `lokation`, `authz_vlan`, `authz_acl`, `platform_type`) og bruger feltet `static_group` (ikke `static_group_assignment`). Begge nøgler eksisterer dermed ikke i snapshot'et — `custom_attributes` faldt tilbage til `{}`. Siden build 0064 sender `set_custom_attributes` faktisk tomme strings til ISE i stedet for at filtrere dem væk (fix for "kunne ikke rydde CA"-buggen), så rollback'en endte med eksplicit at *rydde* alle felter på ISE i stedet for at restore dem. **Løsning** (build 0072): `_endpoint_update_from_snapshot` rekonstruerer nu `CustomAttrs` fra de fladede snapshot-felter og læser `static_group` med det korrekte navn.
- `[fixed] 2026-04-24 — Browse/Edit kan ikke rydde custom attributes (værdien forbliver, vises som "bypass")` — I Browse/Edit detail-modal kunne man ikke sætte nogen af custom attribute-dropdowns (Type, Owner, Lokation, AuthzVlan, AuthzACL, PlatformType) til "—" (tom): efter Gem vendte den forrige værdi tilbage, så endpoint endte i authz-policyens fallback/"bypass"-regel i stedet for den rene tilstand brugeren ønskede. **Rodårsag**: `IseEndpointRepository.update` i [backend/app/ise/endpoints.py](backend/app/ise/endpoints.py) og `OpenApiEndpointRepository.update` i [backend/app/ise/openapi_endpoints.py](backend/app/ise/openapi_endpoints.py) filtrerede empty-string-værdier væk før PUT-payloaden blev sendt til ISE (`non_empty = {k: v for k, v in custom_attributes.items() if v}`). ISE merger `customAttributes`-blokken på PUT (samme opførsel vi allerede havde fikset i `set_custom_attributes` i build 0058), så en droppet nøgle = behold forrige værdi. **Løsning** (build 0064): begge `update`-metoder sender nu hele `custom_attributes`-dict'et uden at strippe empty strings, så ISE faktisk rydder feltet når brugeren vælger "—". `create`-stierne bevarer filteret — der giver det stadig mening ikke at sende tomme felter på en nyoprettet endpoint.
- `[fixed] 2026-04-21 — Slettet attribut-værdi kommer tilbage efter sync fra ISE` — Når en værdi blev fjernet i Attribut-administrationen (f.eks. "hønsehus" i Owner), rapporterede UI "ryddet 1 i ISE", men en efterfølgende "Sync fra ISE" fandt værdien igen. **Rodårsag**: ISE ERS **merger** `customAttributes`-blokken på PUT i stedet for at erstatte den. `set_custom_attributes` i [backend/app/ise/endpoints.py](backend/app/ise/endpoints.py) droppede den fjernede nøgle fra payloaden (og filtrerede i øvrigt alle empty-string-værdier væk) — så ISE beholdt bare den gamle værdi. Docstring hævdede at "omitted keys are cleared on ISE", men det er netop ikke sådan ISE ERS opfører sig. **Løsning** (build 0058): (a) `set_custom_attributes` sender nu payload uden at strippe empty strings. (b) `remove_value` i [backend/app/services/custom_attribute_service.py](backend/app/services/custom_attribute_service.py) sætter eksplicit `new_attrs[attr_name] = ""` i stedet for at droppe nøglen, så ISE faktisk rydder feltet. (c) [ISE_API_REFERENCE.md](ISE_API_REFERENCE.md) gotcha-listen udvidet med merge-adfærden.
- `[fixed] 2026-04-21 — Tilknytning (StaticGroupAssignment) ændrer sig ved CSV export + re-import` — Når man eksporterede et endpoint med Tilknytning "Dynamisk" (staticGroupAssignment=false) og importerede CSV-filen igen, blev endpointet ændret til "Statisk". **Rodårsag**: (1) [frontend/js/csv.js](frontend/js/csv.js) `toIseCsv` skrev `StaticGroupAssignment = r.group_name ? "true" : "false"` — afledt af om gruppen var sat, ikke af den faktiske `static_group`-tilstand. (2) `parseIseFormat` læste slet ikke feltet, så importen videresendte ingen værdi. (3) Backend `CreateEndpointRequest` havde intet felt for det, og `create_endpoint` kaldte `self.endpoints.create(...)` uden `static`-argument — default er `True` i både ERS- og Open API-repo. Resultatet: alle re-importerede endpoints blev Statisk, uanset hvad de var før. **Løsning** (build 0056): (a) `toIseCsv` skriver nu `r.static_group` direkte. (b) `parseIseFormat` læser både `StaticGroupAssignment` og `StaticAssignment` (ISE har historisk brugt begge) via ny `parseBoolCell` helper der accepterer true/false/1/0/yes/no case-insensitive. `parseSimpleFormat` returnerer `staticGroup: null` (ikke specificeret). (c) [frontend/js/views/import.js](frontend/js/views/import.js) sender `static_group_assignment` i bulk-create payload når CSV havde kolonnen. (d) [backend/app/schemas/endpoint.py](backend/app/schemas/endpoint.py) `CreateEndpointRequest` fik `static_group_assignment: bool | None = None`. (e) [backend/app/services/endpoint_service.py](backend/app/services/endpoint_service.py) `create_endpoint` og `_overwrite_existing` honorerer flaget — None = bevar eksisterende ved overwrite / default True ved create.
- `[fixed] 2026-04-21 — CSV Export Template: import opdaterer ikke templaten, reset virker ikke` — I Settings → CSV Export Template opdaterede "Importér template fra CSV-fil" ikke preview-feltet, og "Nulstil til standard"-knappen havde ingen synlig effekt. **Rodårsag**: (1) Excel-eksporterede CSV-filer starter med UTF-8 BOM (`\uFEFF`), så `parseTemplateHeader` fik et første kolonnenavn med BOM-prefix (`"\uFEFFMACAddress"`). `extendTemplateWithPortalColumns` sammenlignede exact-match mod `DEFAULT_TEMPLATE` og fandt ikke `MACAddress`, så den tilføjede en duplikat — template blev sat, men visuelt forvirrende. (2) `<input type="file">` fyrer ikke `change` når samme fil vælges igen, så hvis første import silent fejlede kunne brugeren ikke prøve igen uden at vælge en anden fil. Reset nulstillede heller ikke file-input'ets value. **Løsning** (build 0051): (a) [frontend/js/csv.js](frontend/js/csv.js) `parseTemplateHeader` stripper nu BOM med `text.replace(/^\uFEFF/, "")` og kører `stripQuotes` på hver header-celle så quoted ISE-headers normaliseres. (b) [frontend/js/views/settings.js](frontend/js/views/settings.js) handler wrapper nu file-læsning i try/catch så exceptions vises til brugeren, og `e.target.value = ""` i finally-blokken så samme fil kan vælges igen. Reset-handleren nulstiller også file-input'et.
- `[fixed] 2026-04-19 — Browser-reload tvinger nyt login selvom token er gyldigt` — Hver gang frontend bootede (F5 / genåbn fane) blev brugeren sendt til login, også selvom tokenen i `localStorage` stadig var gyldigt og backend kørte uafbrudt. Rodårsag: `/auth/status` lå i `UNAUTH_PATHS` i [frontend/js/api.js](frontend/js/api.js), så Authorization-headeren blev ikke sendt med kaldet. Backend læser header og returnerer `authenticated: false` hvis ingen `Bearer`-prefix — og `app.js` kalder så `auth.clear()`. **Løsning** (build 0042): fjernet `/auth/status` fra `UNAUTH_PATHS` — token sendes nu med så backend kan verificere det. Det er fortsat en public route (dvs. den kræver ikke token, den fortolker bare et hvis det er der).
- `[fixed] 2026-04-19 — CoA reauth fejler med HTTP 401 (HTML login-side)` — MnT CoA-kaldet `/admin/API/mnt/CoA/Reauth/{psn}/{mac}/{type}` returnerer 401 Unauthorized (HTML-body) selvom basic auth credentials er korrekte. Rodårsag: ISE-brugeren har kun rollen `ERS Admin` — MnT REST API'et kræver rollen `MnT Admin` eller `Super Admin`. ERS og MnT er separate admin-grupper i ISE RBAC. **Løsning** (build 0040): (1) `coa.py` fanger nu 401/403 og login-redirects og giver eksplicit dansk besked om at brugeren mangler MnT Admin-rolle; (2) Settings-view har fået en advarsel ved CoA-felterne om det nødvendige rolle-krav; (3) `ISE_API_REFERENCE.md` opdateret med rolle-kravet. Koden virker som den skal — brugeren skal i ISE tildele `MnT Admin` til den konto portalen bruger.
- `[fixed] 2026-04-18 — Export CSV uden selektion eksporterer kun aktuel side` — Når ingen rækker var valgt og ingen client-side filter var aktivt, eksporterede "Export CSV"-knappen kun de endpoints der var på den aktuelle pagination-side (`allRows`), ikke hele datasættet. I filter-mode virkede det (fordi allRows der er hele settet). **Løsning** (build 0034): hvis ingen selektion og ingen filter, henter export-knappen nu alle endpoints via `listAllEndpointDetails()` (eller cache) før CSV bygges. Knappen disables under hentning.
- `[fixed] 2026-04-18 — ERS filter-felter 'name'/'description' returnerer 400` — Browse toolbar havde Name og Description som valgbare felter i server-side ERS-filter, men ISE 3.4 svarer `400 The filter field 'name' is not supported` (samme for description) selvom ERS SDK-docs angiver dem som filtrerbare. **Løsning** (build 0033): server-side filter-dropdown begrænset til `mac` (eneste empirisk fungerende felt). Name/Description kan stadig filtreres client-side via kolonnefilter-rækken. `ISE_API_REFERENCE.md` opdateret med empirisk felt-liste. — Toolbar viste "10 / 10 endpoints" (kun aktuel side) uden filter, selvom paginerings-baren korrekt viste "Side 1 af 3 (30 total)". I filter-mode vistes "18 / 30 endpoints (filtreret)" korrekt. **Løsning** (build 0031): count i server-side pagination viser nu `${allRows.length} / ${totalEndpoints} endpoints` så page-rows vs total fremgår tydeligt.
- `[fixed] 2026-04-18 — POST /api/custom-attributes/sync returnerer 500 (TypeError)` — `sync_from_ise()` kaldte `list_page()` som om den returnerede en liste, men metoden returnerer tuplen `(resources, total)` (ændret i build 0024). Første iteration fik listen selv som `r`, så `r["id"]` fejlede med `TypeError: list indices must be integers or slices, not str`. **Løsning** (build 0027): unpack tuple i `custom_attribute_service.py:68`, brug `total` til at afgøre om alle sider er hentet.
- `[fixed] 2026-04-17 — Filter søger kun i aktuel side, ikke alle endpoints` — Alle kolonnefiltre (MAC, Identity Group, Tilknytning, Description, Type, Owner, Lokation, AuthzVlan) og "Kun portal"-toggle filtrerede kun i den aktuelle sides data. **Løsning** (build 0024): ny backend-route `GET /endpoints/details/all` henter alle endpoints på tværs af ISE-sider. Frontend skifter automatisk til filter-mode (henter alle, client-side filter + paginering) når et filter aktiveres, og tilbage til server-side paginering når alle filtre deaktiveres. Cache sikrer at gentagne filter-ændringer ikke re-fetcher.
- `[fixed] 2026-04-17 — Browse/Edit ignorerer "Default page size" preference` — Indstillingen "Default page size (browse view)" under Frontend preferences gemmes korrekt i localStorage, men Browse/Edit view brugte altid hardkodet `100` i `api.listEndpointDetails(1, 100)`. **Løsning** (build 0022): `browse.js` læser nu `pageSize` fra localStorage via `getPageSize()`.
- `[fixed] 2026-04-17 — Tema-valg slår ikke igennem` — Valg af tema under Frontend preferences gemt i localStorage men blev aldrig anvendt på DOM. **Løsning** (build 0022): tilføjet `applyTheme()`/`initTheme()` i `settings.js`, kaldt ved app-start i `app.js`, og komplet dark mode CSS i `styles.css` via `[data-theme="dark"]` selektorer.
- `[fixed] 2026-04-17 — Save i Browse/Edit sætter altid staticGroupAssignment=true` — Når man redigerede attributter (description, owner osv.) uden at ændre Identity Group, blev tilknytning sat til Statisk fordi group_id altid blev sendt i payload. **Løsning** (build 0015): frontend sender kun `group_id` når gruppen faktisk blev ændret.
- `[fixed] 2026-04-17 — Browse/Edit refresh nulstiller filter` — Hvis man har sat et filter i Browse/Edit og trykker Refresh, vises alle endpoints i stedet for kun dem der matcher filteret. **Løsning** (build 0011): `load()` kalder nu `applyFilter()` i stedet for direkte `renderRows(allRows)`, så filter og portal-toggle bevares efter refresh.
- `[fixed] 2026-04-16 — Custom attributes sættes ikke på endpoints` — To problemer: (1) `ensure_definitions` blev kun kaldt ved sync, ikke automatisk — fikset i build 0004. (2) **ERS stien `/ers/config/endpointcustomattribute` returnerer 404** — ERS API understøtter slet ikke oprettelse af custom attribute-definitioner. **Løsning** (build 0005): skiftet til ISE **Open API** (`/api/v1/endpoint-custom-attribute`) for at oprette definitioner.
- `[fixed] 2026-04-16 — "Location" konflikter med ISE built-in profiler attribut` — ISE returnerer 500: `"LocationLocation is refered in Profielr Rules"` ved forsøg på at oprette custom attribute "Location". ISE har et indbygget profiler-attribut med samme navn. **Løsning** (build 0006): omdøbt til `Lokation` i hele systemet (schema, store, frontend).
