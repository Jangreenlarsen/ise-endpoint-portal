{
  "name": "hypervision-frontend-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright smoke-tests for HyperVision ISE Portal frontend (mocket backend-API).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
