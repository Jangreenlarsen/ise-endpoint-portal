# Features

Alle nye features registreres her FØR implementering påbegyndes.

**Format**: `[status] YYYY-MM-DD — Titel` — beskrivelse, berørte lag.
**Status**: `planned` · `in-progress` · `done`

---

## Planlagt

- `[done 3.15.0] 2026-05-05 — Portal system opdatering via admin UI` — Admin kan uploade en ZIP-opdateringspakke direkte i Settings → Opdatering. Pakken valideres (version.json påkrævet, path-traversal og .env blokeres), preview viser hvilke filer der opdateres + blokerede filer. Anvend skriver filer til disk øjeblikkeligt (frontend aktivt med det samme). Genstart-knap kalder os._exit(0) og START.bat (nu loop-baseret) genstarter serveren. Kun tilgængelig for admin-brugere. **Lag:** backend (`services/update_service.py` ny, `api/update.py` ny, `main.py`), frontend (`api.js` +validateUpdate/applyUpdate/restartServer, `views/settings.js` +Opdatering-tab), `START.bat` (loop-genstart).

- `[done 3.14.0] 2026-05-05 — Performance: fuld pre-warm cache + offline disk-cache` — Baggrunds PrewarmWorker scanner alle ISE endpoints ved startup og holder cachen varm med konfigurerbar concurrency (default 10). Disk-persistens (JSON) gemmer cachen ved shutdown/scan og loader den ved næste start så Browse viser data øjeblikkeligt — rækker med gammel cache markeres med ⏱-badge. Edit-modal kalder alltid force_fresh og skyder endpoint til hot-queue. Interval-rescan default 30 min. **Lag:** backend (`services/cache_prewarm.py` ny, `core/endpoint_cache.py` +disk_persistence/from_disk, `core/config.py` +3 settings, `schemas/endpoint.py` +cache_stale, `services/endpoint_service.py` +force_fresh, `api/endpoints.py` +prioritize route, `main.py` +lifespan), frontend (`api.js` +prioritizeEndpoint, `views/browse.js` +stale-badge+prioritize, `css/styles.css` +stale-badge styling).

- `[done 3.12.0] 2026-05-03 — Auto-select brugerens egen System adm-rolle ved opret endpoint` — Når en bruger åbner Register-viewet og rollen er tilgængelig i kataloget, pre-selektes chippen med navn = brugerens username automatisk. Brugeren kan fravælge den eller vælge yderligere roller. **Lag:** frontend (`register.js`).

- `[done 3.13.0] 2026-05-03 — PSK mode-type: IPSK vs MPSK i portal-settings` — Ny indstilling i PSK-politik-kortet (Settings) hvor admin vælger om portalen kører i MPSK- eller IPSK-mode. I IPSK-mode tilføjer backend automatisk "psk="-prefix på nøglen ved write til ISE og stripper det igen ved read, så UI-brugeren aldrig ser præfikset. **Lag**: backend (schemas/settings +psk_type, core/config +psk_type, services/settings_service +psk_type i get/update, services/endpoint_service +_psk_encode/_psk_decode), frontend (settings.js +radio-buttons).

- `[done 3.11.0] 2026-05-03 — MPSK/IPSK PSK-nøgle-management` — Ny `editor-psk`-rolle der kan se umaskerede PSK-nøgler og redigere PSK-felter på endpoints. ISE custom attributes `PSK_Mode` (boolean streng) og `PSK_Key` (fri streng) tilføjet til `CustomAttrs` og `EndpointDetail`. PSK-politik (minimum længde, krav om store bogstaver/tal/special) konfigureres i Settings og håndhæves ved create/update. Nøgle-generator i detail-modal og Settings-PSK-politikskort. PSK_Key maskeres til `****` for brugere uden `editor-psk`- eller admin-rolle. Sentinel-værdien `****` afvises ved write-back. **Lag**: backend (`schemas/user.py` +editor-psk, `api/deps.py` +require_psk_editor/require_edit_endpoint, `schemas/endpoint.py` +PSK-felter, `schemas/settings.py` +PskPolicy/GeneratedPskKey, `core/config.py` +PSK-policy-felter, `core/custom_attr_store.py` +PSK_ATTRS, `services/settings_service.py` +get/update/validate/generate_psk_key, `services/endpoint_service.py` +masking/_validate_psk, `api/settings.py` +psk_router, `api/endpoints.py` +is_psk_editor), frontend (`api.js` +getPskPolicy/updatePskPolicy/generatePskKey, `views/browse.js` +PSK UI i detail-modal, `views/settings.js` +PSK-politik-tab + editor-psk i rolle-dropdown).

- `[done 3.8.0] 2026-04-30 — System adm: omdøb endpoint-roller + auto-rolle pr. bruger` — UI-omdøbning af "Endpoint-roller" / "Roller" til "System adm" alle steder hvor portal refererer til endpoint-tag-systemet (Settings, Browse, Register, detail-modal, bulk-edit-modal). Backend-feltnavne, API-stier og ISE-CA-navnet `HypervisionRoles` bevarede for at undgå breaking changes. **Ny funktion:** når en bruger oprettes (eller findes ved startup-backfill) tilføjes automatisk en System adm-rolle til kataloget med navn = brugerens username, så admin kan se den i UI-listen og bruge den som scope-tag på endpoints. Tidligere var username-tag implicit (eksisterede kun hvis admin manuelt skrev navnet ind). Portal-admin (system-rolle = admin) har stadig ingen System adm tilknyttet og ser derfor alt — det er admin der fordeler System adm-tags til endpoints. **Lag**: backend (core/role_catalog +ensure_user_role/backfill, services/user_service.create_user hook, main.py lifespan-backfill), frontend (settings.js, browse.js, register.js UI-rename).

- `[done 3.7.1] 2026-04-29 — Purge-protection via guide-card (ISE-config kan ikke automatiseres)` — Rollback af 3.7.0's DRS-stempling efter at det viste sig (a) ikke at virke som purge-bypass på ISE 3.4 (custom attributes må ikke bruges som purge-condition på den version), og (b) at være overflødig på ISE 3.5+ hvor admin kan lave en Never-Purge-rule direkte med `CUSTOMATTRIBUTE HypervisionISEPortal EQUALS true` (en CA portalen allerede sætter siden 2.x). Erstattet med Settings UI guide-card der dokumenterer den manuelle ISE-konfiguration step-by-step + ISE_API_REFERENCE.md udvidet med stort afsnit om pxGrid 2.0 empiriske erfaringer og purge-policy mekanikker. **Lag**: backend (revert custom_attr_store, endpoint_service, api/endpoints), frontend (api.js revert, settings.js erstat backfill-button med guide+kopiér-knapper), docs (ISE_API_REFERENCE.md). **Note:** der findes IKKE ERS eller Open API til purge-rules — det er ren GUI-config også i ISE 3.5.

- `[done 3.7.0] 2026-04-29 — Purge-protection: DeviceRegistrationStatus="Registered" på alle portal-endpoints` — ISE's default endpoint-purge-policy fjerner endpoints baseret på inaktivitet, hvilket er ødelæggende for portal-managed endpoints (statisk registrerede devices der ikke nødvendigvis ses ofte på netværket). ISE's purge-rules springer endpoints over hvor `DeviceRegistrationStatus="Registered"` — den indbyggede attribut er forbeholdt BYOD-flowet, men purge-evalueringen matcher også custom attributes med samme navn. Implementering: (a) ny CA `DeviceRegistrationStatus` tilføjet til HIDDEN_ATTRS så ISE-definition auto-bootstrappes ved første create/update; (b) endpoint_service stempler automatisk værdien `Registered` på alle create + update (sammen med eksisterende `HypervisionISEPortal=true`); (c) ny admin-only API `POST /api/endpoints/purge-protect-backfill` der scanner alle portal-endpoints (HypervisionISEPortal=true) og stempler Registered på dem der mangler det — idempotent, kan køres flere gange. (d) Settings UI har nu en "Purge-protection"-card med Backfill-knap og resultat-rapport (scanned/already_ok/updated/failures). **Lag**: backend (custom_attr_store +PURGE_PROTECT_*, endpoint_service create/update + purge_protect_backfill, api/endpoints), frontend (api.js, settings view).

- `[done 3.6.0] 2026-04-29 — PxGrid Phase 4: endpoint-topic + live cache-invalidering` — Worker subscriber nu også til `com.cisco.ise.endpoint`-topic via en anden sub-id på samme WebSocket — admin-ændringer i ISE-GUI invaliderer 2.8.0 endpoint-cache i real-time og pushes til Browse-viewet som `endpoint_changed`-event på den eksisterende SSE-stream. **Backend**: refactor af `session_worker._one_session()` til multi-SUBSCRIBE med routing via `subscription`-header (sub-session vs sub-endpoint). Ny `_handle_endpoint_body()` parser tolerant for forskellige payload-shapes, kalder `endpoint_cache.invalidate_detail(id)` (eller `invalidate_all()` ved manglende ID), broadcaster på samme SSE-bus. Worker-status separerer nu `session_events_total` og `endpoint_events_total`. **Settings**: opt-in toggle `pxgrid_endpoint_topic_enabled` + custom topic-felt (default `/topic/com.cisco.ise.endpoint`). Worker restartes automatisk ved settings-save så ny subscription tager effekt. **Frontend**: `browse.js` lytter på `endpoint_changed`-events og trigger debounced (500ms) reload — skipper hvis bruger er midt i edit (dirtyIds non-empty) så lokale ændringer ikke overskrives. Settings worker-status viser nu separate event-tællere for de to topics.

- `[done 3.5.0] 2026-04-29 — PxGrid Phase 3: SSE-stream til frontend (live auth-status uden poll)` — Lukker loopet fra Phase 2b: cache-deltas streames direkte til Browse-viewet via Server-Sent Events. **Backend**: `session_cache` får pubsub-fan-out (asyncio.Queue pr. subscriber, drop-oldest ved overflow). Nyt endpoint `GET /api/pxgrid/sessions/stream?token=<jwt>` (text/event-stream, JWT i query fordi EventSource ikke kan sætte headers; valideres mod samme codepath som require_any). Sender `snapshot` ved connect, derefter `upsert/remove/clear` events + `keepalive` hvert 15s. **Frontend**: `browse.js` åbner EventSource ved view-load og holder lokal MAC-set live; når pxGrid-stream er live springes MnT-polden over. Auto-reconnect via EventSource; falder tilbage til MnT hvis stream dropper. Cleanup på view-skift. **Designvalg**: SSE over WebSocket fordi one-way push er nok, browser-side auto-reconnect er gratis, ingen ekstra dep. **Næste fase (3.6.0)**: Phase 4 — `com.cisco.ise.endpoint`-topic + invalidér 2.8.0 endpoint-cache i real-time.

- `[done 3.4.0] 2026-04-28 — PxGrid Phase 2b: Persistent STOMP-worker + in-memory session-cache` — Bygger ovenpå Phase 2a's verificerede WS+STOMP-lag og leverer den permanente kanal: lifespan-task der subscriber uendeligt til `com.cisco.ise.session`, parser MESSAGE-frames og holder MAC→SessionInfo-cache opdateret. Auto-reconnect med eksponentiel backoff (1→300s default), PSN-failover via fresh ServiceLookup pr. cycle, heartbeat-tab → reconnect, fresh AccessSecret pr. reconnect. **Tunables i Settings**: heartbeat_ms, reconnect_min/max_s, cache max age, worker enabled (separat fra pxgrid_enabled), session topic. Worker restartes automatisk ved settings-save så ændringer tager effekt uden backend-restart. **API**: `GET /api/pxgrid/sessions`, `/sessions/{mac}`, `/worker/status`, `POST /worker/restart`. **Frontend**: nyt fieldset i PxGrid-card med alle tunables + live worker-status (running/connected, peer, events, cache-size, reconnects, last error) som auto-refresher hvert 10s. **Næste fase (3.5.0)**: Phase 3 — SSE-stream til frontend så `browse.js` får real-time auth-status uden poll.

- `[done 3.3.0] 2026-04-27 — PxGrid Phase 2a: Minimal STOMP-prober (verificér WebSocket-laget før fuld session-cache)` — Når portalens pxGrid-bootstrap (REST på port 8910) er på plads, er næste step at bevise at WebSocket+STOMP-laget mod pubsub-noden også fungerer end-to-end *før* vi investerer i fuld session-cache + SSE-stream til frontend. Bygger ovenpå `client.service_lookup("com.cisco.ise.pubsub")` + `access_secret_create()` som allerede er implementeret. **Mål**: knap i Settings der starter en N-sekunders subscribe-session mod `/topic/com.cisco.ise.session` og rapporterer "Modtog X MESSAGE-frames + sample-payload" — bruges som diagnostik-værktøj og første-trin-validering. **Arkitektur**: (1) Nyt `backend/app/pxgrid/stomp.py` med tiny STOMP 1.2 frame-encoder/decoder (ingen ekstern dep, ~100 LOC). (2) Nyt `backend/app/pxgrid/probe.py` med `run_session_probe(duration_s)` der walker: AccessSecretCreate → WebSocket connect (mTLS, samme cert som control-plane) → STOMP CONNECT (login=node, passcode=secret, heart-beat=0,30000) → SUBSCRIBE → læs frames i `duration_s` sekunder med asyncio.wait_for-timeout → DISCONNECT → luk WS. Returnerer struktureret rapport `{ok, step, messages_received, sample_payloads (≤3), duration_s, error}`. (3) Nyt endpoint `POST /api/settings/pxgrid/stomp-probe?duration=10` (default 10s, max 60s). (4) Frontend-knap "Test STOMP-subscription (10s)" under eksisterende "Test forbindelse" med inline-resultat. (5) Eksplicit `websockets>=12.0` i `pyproject.toml` (transitiv via uvicorn[standard] men gør det synligt). **Designvalg**: Probe er *read-only og selvterminerende* — hverken cache eller SSE-stream skrives til, så det er sikkert at trigger igen. Tom-resultat (0 events i 10s) er IKKE en fejl — RADIUS-trafik kan være lav. Rapporterer `error_step` (cert/connect/subscribe/timeout) så fejlsøgning er præcis. **Lag**: backend (ny `pxgrid/stomp.py`, `pxgrid/probe.py`, `schemas/settings.py` +PxGridStompProbeResponse, `api/settings.py` +endpoint, `services/settings_service.py` +probe-wrapper), frontend (api.js +runStompProbe, views/settings.js +knap+resultat). **Bump-begrundelse**: MINOR — nyt API + UI, additive, non-breaking. **Næste skridt efter 3.3.0**: Phase 2b (persistent STOMP-worker + in-memory session-cache) og Phase 2c (SSE-stream til frontend).

- `[done 3.2.0] 2026-04-27 — PxGrid: "Nulstil registrering"-knap i Settings` — Admin-knap der nulstiller portal-side pxGrid-registrering så CSR-flowet kan køres forfra uden manuel oprydning på disken. Sletter `backend/pxgrid/<node>.{cert,key,ca,csr}.pem`, rydder cert/key/CA-paths + gemt password fra settings, men **beholder** config-niveau felter (enabled, node_name, psn_fqdn, cert_mode) så admin ikke skal indtaste dem igen. Use cases: server-skift (klient-entry på gamle ISE matcher ikke længere), forkert cert uploadet, expired keys, eller bare at starte rent under fejlsøgning. Idempotent. **Lag**: backend (ny `cert_manager.delete_artifacts()`, `services/settings_service.pxgrid_reset()`, `POST /api/settings/pxgrid/reset` + `PxGridResetResponse`-schema, audit-event `reset/backend_settings/pxgrid`), frontend (rød "Nulstil registrering"-knap nederst i pxGrid-kortet med confirm-dialog der lister hvad der slettes/beholdes; reload settings efter success). Reset rører ikke ISE-siden — admin skal selv slette klient-entry'en i ISE Clients-listen hvis 100% rent flow ønskes; det er noteret i både API-docstring og UI-hint.

- `[done 2.9.0] 2026-04-24 — Audit log + rollback` — Append-only historik over alle write-operationer (endpoint create/update/delete, custom attribut-værdier, DACL CRUD, RBAC-ændringer, settings) med aktør, tidspunkt, før/efter-diff. Én-klik rollback fra historik-viewet. **Mål**: compliance + accountability i multi-bruger deployments; `app.log` er i dag eneste historik og den er ikke struktureret til rollback. **Arkitektur (fases)**: **Phase 1 — Audit store**: `backend/app/core/audit_store.py` med SQLite (append-only; valgt over JSON-fil pga. query-behov ved filter). Skema: `{id, ts, actor_id, actor_username, action, resource_type, resource_id, before_json, after_json, source_ip}`. Retention-policy (default 90 dage, konfigurerbar) + daglig prune-task. **Phase 2 — Write-path instrumentering**: alle services (endpoint, custom_attribute, dacl, user, settings) publisher audit-event synkront *efter* vellykket ISE-kald eller local-store-write. Dekorator `@audited(resource_type)` der auto-capturer before (via `get`) og after (via function args). **Phase 3 — API**: `GET /api/audit` med filter (actor, resource_type, resource_id, from/to), pagineret. `POST /api/audit/{id}/rollback` valideret: kun admin-rolle; afviser hvis resource mellemtiden er slettet eller før-tilstanden er ukendt. **Phase 4 — Frontend**: ny "Audit"-side i sidebar med tabel + filtre + diff-viewer (side-ved-side JSON). Per-endpoint "Historik"-knap i detail-modal viser kun ændringer for det endpoint. Rollback-knap bag confirm-dialog. **Settings**: `audit_enabled` (default true), `audit_retention_days`, `audit_prune_hour`. **Lag**: backend (ny core/audit_store, schemas/audit, api/audit, services-dekoratorer), frontend (ny audit view, api.js, detail-modal). **Non-breaking MINOR**.

- `[done 2.10.0] 2026-04-25 — Ny RBAC-rolle "registrar" + mobile onboarding-view` — Fjerde rolle oveni eksisterende `admin / editor / viewer`: `registrar` må **kun** oprette endpoints — ingen browse, edit, delete, settings, audit eller DACL-adgang. Tænkt til field-teknikere der går rundt i produktion med mobiltelefon og skal registrere nye devices on-the-spot. **Arkitektur (fases)**: **Phase 1 — Backend RBAC-udvidelse**: `schemas/user.py` `Role = Literal["admin", "editor", "viewer", "registrar"]`, `ROLE_VALUES` opdateres. `api/deps.py` ny `require_registrar_or_above = require_roles("admin", "editor", "registrar")`. Kun `POST /api/endpoints` og `GET /api/groups` + `GET /api/custom-attributes` (dropdowns) må kaldes af registrar; alt andet giver 403. **Phase 2 — Mobile-optimeret registreringsview**: ny route `/#register` (eller selvstændig simplificeret app-bundle) med kun MAC-input (auto-detekterer format), Identity Group-dropdown, Type/Owner/Lokation-dropdowns, Submit. Ingen sidebar, ingen side-menu. Responsive CSS prioriteret mobile-first: store touch-targets (min 44px), stor font, fuldskærms-form. **Phase 3 — MAC-input-hjælpere**: (a) camera-scan af MAC QR/barcode via `navigator.mediaDevices` + jsQR library; (b) NFC read hvor understøttet (Chrome Android); (c) manuel paste med auto-format. **Phase 4 — PWA**: manifest.json + service-worker så registrar-viewet kan installeres som app på iPhone/Android home screen; offline-kø der gemmer submissions lokalt og retry'er når netværk er tilbage. **Phase 5 — Auto-login for field-devices (optional)**: QR-kode i admin-UI der indeholder en short-lived device-token så tech ikke skal indtaste password hver gang. **Lag**: backend (schemas/user, api/deps, api/endpoints guard, evt. api/auth device-token), frontend (ny register view, router, mobile CSS, PWA manifest, service-worker, jsQR integration). **Non-breaking MINOR** — eksisterende roller uændret.

- `[done 2.11.0] 2026-04-25 — MAC OUI → vendor lookup (auto-enrichment)` — Brug første 24 bit af MAC-adresse (OUI) til at slå vendor-navn op mod en offline IEEE OUI-database; vis vendor som inline badge i Browse-tabellen og brug det til at auto-foreslå `Type` / `PlatformType` i Create-formularen (f.eks. MAC der starter med Cisco OUI → foreslå `iosxe`). **Arkitektur (fases)**: **Phase 1 — OUI-database**: bundle `backend/data/oui.csv` (IEEE public MA-L + MA-M + MA-S registries, ~35k entries, ~2 MB). Loades som dict `{"001122": "Cisco Systems"}` ved FastAPI-startup. Genereringsscript `scripts/update_oui.py` henter seneste data fra IEEE og commiter. **Phase 2 — Lookup-service**: `backend/app/core/oui_lookup.py` med `lookup(mac) -> vendor | None`; håndterer MA-L (24 bit), MA-M (28 bit), MA-S (36 bit) med fallback-rækkefølge. **Phase 3 — Berigelse af endpoint-responses**: `EndpointDetail` + `EndpointSummary` får nyt felt `vendor: str = ""` (ikke gemt i ISE, udledt); udfyldes i `endpoint_service.py` ved list/get. **Phase 4 — Frontend visning**: Browse-tabel viser vendor som lille grå badge ved siden af MAC; CSV-export inkluderer vendor som ekstra kolonne (read-only, udledes ved import). **Phase 5 — Auto-suggest i Create**: når bruger indtaster MAC → frontend-opslag mod `/api/oui/{mac}` → suggest-badge "Detekteret: Cisco — sæt PlatformType=iosxe?" med én-klik-accept. Mapping OUI → foreslået PlatformType er en lille tabel i backend der kan redigeres i Settings. **Settings**: `oui_enabled`, `oui_auto_suggest`, `oui_data_version` (vist read-only). **Lag**: backend (ny core/oui_lookup + data/oui.csv, schemas/endpoint +vendor, services/endpoint_service berigelse, ny api/oui), frontend (browse view badge, create view suggest, settings OUI-version). **Non-breaking MINOR** — ingen ISE-impact.

- `[done 2.12.0] 2026-04-25 — Endpoint-level RBAC (per-bruger endpoint-roller)` — Udvidelse af RBAC så ikke-admin-brugere kun ser de endpoints der er tagget med deres tildelte roller. Admin definerer et katalog af **endpoint-roller** (fx `alle-Printer`, `PLC-HalA`, `CTS`) — disse er IKKE bruger-roller men et tagging-system på endpoints. Admin tildeler N roller til hver bruger. Hver bruger har implicit også sit eget username som rolle. Bruger ser et endpoint hvis dets `HypervisionRoles`-CA indeholder mindst én af brugerens **effektive roller** (`assigned + [username]`). Eksempler: (a) `svendbent` har `alle-Printer` + implicit `svendbent` → ser endpoints tagget med en af de to. (b) `Henning` har `PLC-HalA` + `CTS` + implicit `henning` → ser endpoints tagget med en af de tre. (c) `admin` ser alt uanset tag. **Storage**: Ny ISE custom attribute `HypervisionRoles` (string, comma-separated rolle-navne) bootstrappet på samme vilkår som `HypervisionISEPortal` (HIDDEN_ATTR i [backend/app/core/custom_attr_store.py](backend/app/core/custom_attr_store.py)). ISE er single source of truth for endpoint→rolle-tilknytning. **Designvalg (lukket m. brugeren 2026-04-25)**: (1) Editor/viewer uden tildelte roller ser KUN endpoints tagget med deres eget username — tomt syn er forvirrende men eksplicit. (2) Eksisterende endpoints uden tag bliver usynlige for non-admin (least-privilege); admin får inline edit-kolonne i Browse/Edit til at tagge de endpoints han vil. (3) Registrar ser både eget username-tag og assigned roles, plus en "Mine endpoints"-knap i bunden af registreringssiden der viser en mobil-venlig liste i samme stil. **Arkitektur (fases)**: **Phase 1 — ISE CA-bootstrap**: udvid `HIDDEN_ATTRS` med `HypervisionRoles`; `_ensure_ca_definitions()` opretter den i ISE ved første kald hvis ikke allerede til stede. **Phase 2 — Rolle-katalog**: nyt `core/role_catalog.py` med `endpoint_roles.json` (`{name, description, created_by, created_at}`). CRUD-API `/api/endpoint-roles` (admin only for write, alle for read). **Phase 3 — User-role assignments**: udvid `User`-schema med `assigned_endpoint_roles: list[str]` (default `[]`). `users.json`-migration ved load (auto-fyld feltet). Ny endpoint `PUT /api/users/{username}/endpoint-roles` (admin only). `GET /api/auth/me` returnerer effektive roller. **Phase 4 — Read-path filter**: i `EndpointService.list_endpoints` og `get_endpoint`, hvis `user.role != "admin"`: hent effektive roller, parse `HypervisionRoles` på hvert endpoint, kassér rækker uden match. Ved direkte `GET /endpoints/{id}` udenfor scope returneres 404 (ikke 403, så scope-grænsen ikke leakes). Bulk-export og MnT-session-MAC-list ligeledes filtreret. **Phase 5 — Write-path scope-binding**: når et endpoint oprettes/opdateres af non-admin, sættes `HypervisionRoles` automatisk til at indeholde brugerens username hvis ingen roller eksplicit er valgt. Admin/editor kan vælge yderligere roller fra rolle-kataloget via multi-select. Registrar's auto-tag bruger deres eget username. **Phase 6 — Frontend** (3 sub-views): (a) **Settings → "Endpoint-roller"**: tabel + opret/slet (admin); (b) **Settings → "Brugere"**: per-bruger multi-select for roller (admin); (c) **Browse/Edit**: ny kolonne "Roller" med inline multi-select edit (kun admin/editor); (d) **Register**: rolle-multi-select skjult for registrar (auto-tag username); admin/editor kan vælge fra kataloget; (e) **Register → "Mine endpoints"**: ny knap i bunden der åbner en mobil-venlig liste over endpoints brugeren har adgang til (samme look som register-siden, simpelt MAC + group + roller, ingen edit). **Phase 7 — Audit**: alle CRUD af roller (`endpoint_role`-resource) og role-assignments (`user_roles`-resource) logges i audit-store. **Lag**: backend (`core/role_catalog`, `schemas/user` +assigned_endpoint_roles, `schemas/endpoint_role`, `api/endpoint_roles`, `api/auth/me`, `services/endpoint_service` filter+autotag, `core/custom_attr_store` HIDDEN_ATTRS), frontend (settings view +roller+brugere, browse view +Roller-kolonne, register view +picker+mine-endpoints). **Bump-begrundelse**: MINOR — additive, non-breaking. Eksisterende deployments uden role-tilknytning vil opføre sig som hidtil for admin (alt synligt); editor/viewer/registrar starter med tomt syn indtil admin tildeler.

- `[planned 2.13.0] 2026-04-22 — Webhooks til ServiceNow CMDB (+ generiske)` — Portalen fyrer HTTP POST mod konfigurerbare webhook-URLs ved endpoint-events (create / update / delete). Primær use-case: automatisk sync til **ServiceNow CMDB** så CI-records følger portalens endpoints uden manuel dobbelt-registrering. **Arkitektur (fases)**: **Phase 1 — Webhook-registry**: `backend/webhooks.json` med liste af `{id, url, event_types, template, auth, enabled}`. CRUD i Settings-view. **Phase 2 — Dispatcher**: `backend/app/services/webhook_service.py` asyncio-task-kø; hver event lægger jobs på køen, workers POST'er med exponential backoff (1s, 5s, 30s, 5min, giv op efter 5 forsøg). **Phase 3 — Payload-templates**: (a) **ServiceNow CMDB**-template: pre-formateret til `cmdb_ci_computer` / `cmdb_ci_ip_device` med felter `mac_address`, `name`, `company`, `location`, `asset_tag`; mapping fra portal → SNOW-felter konfigurerbar i Settings. (b) **Generisk JSON**-template: hele endpoint-diff'en som `{event, before, after, actor, ts}`. (c) Valgfri **HMAC-SHA256-signatur** i `X-Portal-Signature`-header så modtager kan verificere. **Phase 4 — Auth til ServiceNow**: basic auth / OAuth 2.0 client credentials (SNOW understøtter begge); credentials i secrets store (udvid eksisterende settings-encrypt-pattern). **Phase 5 — Delivery log + retry-UI**: alle leveringer logges i audit-store (kræver 2.9.0). Settings-view viser "Seneste leveringer" med status (sent / failed / pending) + re-send-knap for fejlede. **Phase 6 — Incoming webhooks (stretch)**: `POST /api/webhooks/incoming/{id}` så eksterne systemer kan pushe endpoint-ændringer tilbage til portalen (fx IPAM der tildeler VLAN). **Settings**: per-webhook URL, events, template, auth, signing-secret, enabled. **Lag**: backend (ny services/webhook_service, schemas/webhook, api/webhooks, core/webhook_store, audit-integration), frontend (settings webhook-sektion, delivery-log view). **Non-breaking MINOR** — afhænger af 2.9.0 (audit-log til delivery-historik).

- `[done 3.9.0] 2026-05-01 — Saved filter views (per-bruger Browse-presets)` — Browse-toolbar har ny "📁 Views ▾"-dropdown der gemmer nuværende filterkombination (Kun portal, server-side MAC-filter, kolonnefiltre) som navngivet view pr. bruger. Én-klik genvælg fra menuen. **Backend**: ny `api/me`-router med CRUD `GET/POST/PUT/DELETE /api/me/views`, max 20 views pr. bruger, gemt i `users.json` under `saved_views[]`-feltet. Audit-logged som `saved_view`-resource. **Frontend**: `applyFilterSnapshot()` factored ud af `restoreFilters()` så samme apply-logic genbruges af både localStorage-restore og view-aktivering. Dropdown inkluderer "💾 Gem nuværende filtre som view…"-action der prompter for navn. Endpoint-templates fra original 2.14.0-plan er udsat til separat release. **Lag**: backend (schemas/user +SavedView*, ny api/me.py, main.py +include_router), frontend (api.js +listMyViews/createMyView/updateMyView/deleteMyView, browse.js +Views-dropdown + reloadViews/renderViewsMenu + apply/save/delete handlers, styles.css +.views-* rules + dark theme).

- `[planned 2.14.0] 2026-04-22 — Endpoint-templates (Phase 3-5 fra 2.14.0-planen)` — Pre-udfyld Create-formularen fra navngivne skabeloner ("ESP32-Modbus", "Meraki AP", "Canon printer") med default Type/Owner/Lokation/AuthzVlan/Group. Adskilt fra Saved Views (3.9.0) fordi de hænger løst sammen og kan landes uafhængigt. Backend: `backend/templates.json` (delt katalog, ikke per-bruger; admin-only write) med `{id, name, fields}`, CRUD via `/api/templates`. Frontend: Create-view dropdown "📋 Skabelon ▾" der pre-udfylder felter; Settings template-editor for admin. — To relaterede UX-features: (a) **Saved views** — gem nuværende filter-kombination (kolonnefiltre, server-MAC-filter, "Kun portal"-toggle, page-size) som navngivet view pr. bruger; én-klik genvælg fra dropdown i Browse-toolbar. (b) **Endpoint-templates** — pre-udfyld Create-formularen fra navngivne skabeloner ("ESP32-Modbus", "Meraki AP", "Canon printer") med default Type/Owner/Lokation/AuthzVlan/Group. **Arkitektur (fases)**: **Phase 1 — Saved views backend**: per-bruger views-liste gemt i `user_store` (udvid user-record med `saved_views: list[{name, query, created_at}]`); max 20 pr. bruger. CRUD via `/api/me/views`. **Phase 2 — Saved views frontend**: Browse-toolbar får dropdown "📁 Views ▾" med gemte views + "Gem nuværende..." action. Filter-state serialiseres som URL-query-string så views kan deles via link (`/#browse?view=mine-aps` eller rå query). **Phase 3 — Templates backend**: `backend/templates.json` (delt, ikke per-bruger; kun admins kan redigere) med `{id, name, fields}`. CRUD via `/api/templates` (read = alle, write = admin/editor). **Phase 4 — Templates frontend**: Create-view får dropdown "📋 Skabelon ▾" øverst — vælg én → alle default-felter pre-udfyldes, bruger retter kun MAC (og evt. override). Registrar-viewet (2.10.0) bruger udelukkende templates så field-tech ikke skal tænke på attributter — vælg skabelon + scan MAC + submit. **Phase 5 — Template-editor i Settings**: admin-brugere kan CRUD skabeloner via tabel i Settings → "Endpoint-skabeloner". **Lag**: backend (user_store +saved_views, ny core/template_store + schemas/template + api/templates + api/me, udvidelse af User-schema), frontend (browse toolbar views-dropdown + URL-sync, create view template-dropdown, registrar view template-picker, settings template-editor). **Non-breaking MINOR** — hænger løst sammen med 2.10.0 (registrar-flow).

- `[done 2.8.0] 2026-04-24 — Endpoint-cache + background sync (stale-while-revalidate)` — Undgå fuld ISE-refetch ved hvert filter-toggle / Refresh / tab-skift. Nuværende adfærd: hver filter-ændring i Browse/Edit udløser N+1 ISE-kald (list + én GET pr. endpoint); ved 100+ endpoints bliver systemet mærkbart sløvt. **Mål**: cache-serve under 10 ms for hot-data + baggrundsopdatering så brugeren aldrig venter på ISE ved almindelig navigation. **Arkitektur (fases)**: **Phase 1 — Backend endpoint-cache**: nyt `backend/app/core/endpoint_cache.py` med in-memory dict `{endpoint_id → (EndpointDetail, fetched_at)}` + global list-snapshot; TTL (default 60s) + stale-while-revalidate-semantik (svar fra cache hvis fresh; returnér stale + spawn bg-refresh-task ellers); gruppe-cache ligeledes. **Phase 2 — Background sync worker**: FastAPI lifespan-hook starter asyncio-task der kører periodisk fuld scan (default 5 min, konfigurerbart i Settings); inkrementel delta-sync via ERS `lastUpdateTime`-filter så kun endpoints ændret siden sidste sync hentes; nyt `/api/cache/stats` viser hit/miss-rate, last-sync-tid, entry-count, byte-size. **Phase 3 — Cache-invalidering ved write**: `EndpointService.update_endpoint/create_endpoint/delete_endpoint` opdaterer cache synkront efter vellykket ISE-kald så næste læs ser den nye tilstand uden at vente på TTL. Bulk-import invalidere list-snapshot en gang efter hele batchen. **Phase 4 — Frontend stale-while-revalidate**: `browse.js` / `api.js` modtager `X-Cache-Version` og `X-Cache-Age` headers; filter-toggle og kolonne-filter rammer nu cache (0 ISE-kald hvis fresh) i stedet for re-fetch; row-diff ved revalidation så DOM kun opdateres ved faktiske ændringer (undgår flicker). **Phase 5 — Skalering (post-3.0.0 hvis behov)**: flyt cache til Redis hvis multi-worker deployment kræves; ikke aktuelt i nuværende single-instance setup. **Settings**: nye felter `cache_enabled`, `cache_ttl_seconds`, `cache_sync_interval_seconds`, `cache_stale_while_revalidate`. **Lag**: backend (ny `core/endpoint_cache.py`, `services/cache_sync.py`, opdateringer til `services/endpoint_service.py` + `api/endpoints.py`, `main.py` lifespan), frontend (api.js header-handling, browse view diff-render). **Bump-begrundelse**: MINOR — ny infrastruktur men ingen breaking changes; eksisterende API'er bevarer samme shape.

- `[wontfix 3.0.0] 2026-04-26 — PxGrid event-invalidering af endpoint-cache` — **Konklusion efter implementation+test (2026-04-29):** ISE 3.4 og 3.5 publicerer ikke endpoint-CRUD-events via pxGrid 2.0 selv med endpoint-topic enabled (ServiceLookup OK, SUBSCRIBE accepteret, men 0 events ved admin-create/update/delete). Det er Cisco's design — pxGrid 2.0 er forbeholdt session/profiler/ANC-state, ikke config-mirror. Multi-topic worker-infrastruktur fra 3.6.0 er bevaret som opt-in fordi den virker for profiler-drevne ændringer, men "endpoint event-invalidering"-use-casen er opgivet. 2.8.0 background-sync (default 60s) bruges som B-løsning. Detaljer i [ISE_API_REFERENCE.md § pxGrid 2.0 empiriske erfaringer](ISE_API_REFERENCE.md). — Byg videre på 2.8.0's cache: `com.cisco.ise.endpoint`-topic fra PxGrid invaliderer eller opdaterer specifikke cache-entries i real-time når en admin ændrer et endpoint direkte i ISE-GUI. `com.cisco.ise.session`-events pushes som delta til frontend via samme SSE-kanal som session/auth-status (se separat 3.0.0-entry). Kombineret resultat: cache er altid aktuel uden periodisk poll — background-sync-interval kan hæves markant (fra 5 min → 30 min) eller helt slåes fra når PxGrid er aktivt, hvilket reducerer ISE-load. Graceful fallback til TTL-baseret invalidering hvis PxGrid WS dør. Forudsætter 2.8.0 (cache-lag) og 3.0.0 Phase 1-3 (PxGrid infrastruktur + session topic). Lag: backend (nyt `pxgrid/endpoint_topic.py`, integration med `endpoint_cache`), frontend (invalidering-events på `/api/events/*` SSE-stream).

- `[done 3.0.0 → 3.7.3] 2026-04-29 — PxGrid server-push af session/auth-status` — Erstat den pull-baserede MnT-session-liste (interval-poll fra Browse/Edit) med ægte server-push via **Cisco PxGrid 2.0** (WebSocket+STOMP). Portalen reagerer i real-time på `sessionCreated / sessionUpdated / sessionDisconnected`-events fra ISE uden F5 eller poll-timer. **Præ-krav i ISE**: (1) PxGrid service enabled (`Administration → pxGrid Services`), (2) portalen registreret som pxGrid client og approved af admin, (3) X.509 client-cert (enten CSR mod ISE internal CA eller upload af pre-issued cert) + CA-chain for TLS-trust. **Arkitektur (fases)**: **Phase 1 — Infrastructure**: nyt `backend/app/pxgrid/` modul med REST-klient til `/pxgrid/control/*` (AccountCreate → AccountActivate → ServiceLookup → AccessSecretCreate), mTLS via httpx med konfigurerbare cert+key-paths. **Phase 2 — Session subscription**: WebSocket/STOMP-klient abonnerer på `com.cisco.ise.session`-topic, in-memory cache MAC → (state, lastSeen, auditSessionId, nasIp, userName), auto-reconnect + PSN-failover via ServiceLookup. **Phase 3 — Frontend push**: nyt `/api/events/sessions` SSE-endpoint streamer MAC→active/inactive deltas; `browse.js` bytter `getSessionMacs()`-poll til `EventSource`-connection så række-checkbox farves grøn/rød live. Graceful fallback til eksisterende MnT-poll hvis `pxgrid_enabled=false` eller hvis WS-connection fejler persistent. **Phase 4 — udvidelse (post-3.0.0)**: abonnement på `com.cisco.ise.endpoint` (admin-ændringer på endpoints reflekteres live), `com.cisco.ise.anc` (quarantine-status), `com.cisco.ise.mdm` (compliance). **Settings**: nye backend-felter `pxgrid_enabled`, `pxgrid_node_name` (portalens identitet hos ISE), `pxgrid_psn_fqdn`, `pxgrid_cert_path`, `pxgrid_key_path`, `pxgrid_ca_bundle_path`. **Lag**: backend (nyt `pxgrid/` modul med `client.py`+`stomp.py`+`session_cache.py`, `api/events.py`, `core/config`, `services/pxgrid_service`), frontend (api.js, browse view). **MAJOR-bump begrundelse**: første eksterne push-integration + arkitektur-skift fra pure-pull (ERS/MnT) til hybrid pull+push (PxGrid events). Version 3.0.0.

## Aktive / færdige

- `[done 3.1.0] 2026-04-26 — PxGrid: PKCS#12-import (.pfx fra MS certsrv / generic CA)` — Tredje vej til at få cert+key+CA på portalen, ud over (a) tre separate PEM-uploads og (b) CSR-flow mod ISE internal CA. Nyt endpoint `POST /api/settings/pxgrid/pfx` (multipart: `file` + `password`) bruger `cryptography.hazmat.primitives.serialization.pkcs12.load_key_and_certificates()` til at splitte bundlet, persisterer cert/key/(evt. CA-chain) som separate PEMs under `backend/pxgrid/<node>.{cert,key,ca}.pem`, og opdaterer alle tre `pxgrid_*_path`-settings i ét hug. Use-case: admin går ind på MS certsrv (`https://<adcs>/certsrv`), submitter en CSR (eller får en pre-issued cert), klikker "Install Certificate" → eksportér med "Yes, export the private key" + "Include all certificates in path", og uploader hele bundlet i Settings. Sparer både CSR-genereringen og chain-jagten. Failure-modes: dårligt password / korrupt PFX giver `400` med dansk besked; bundle uden private key afvises eksplicit. UI: ny PFX-sektion under upload-blokken med fil + password-felt + eksplicit "Importér PKCS#12"-knap (ikke auto-submit på fil-change, da password kræves). Lag: backend (api/settings nyt endpoint, pxgrid/cert_manager nye `extract_pkcs12` + `save_pkcs12_bundle` helpers), frontend (api.js multipart-upload, views/settings.js PFX-sektion). MINOR-bump (additive, non-breaking).

- `[done 3.0.2] 2026-04-26 — PxGrid CSR-download fra Settings UI` — Efter `POST /api/settings/pxgrid/csr` lå CSR-filen kun på serverens disk under `backend/pxgrid/<node>.csr.pem` — admin måtte SSH/RDP ind for at hente den og indsende til ISE internal CA. Nyt endpoint `GET /api/settings/pxgrid/csr/download` streamer filen tilbage som `application/x-pem-file`-attachment. UI: ny "Download CSR-fil"-knap i CSR-blokken + auto-download udløses straks efter "Generér CSR" så filen lander i Downloads uden ekstra klik. 404 hvis CSR ikke er genereret endnu. Lag: backend (api/settings, pxgrid/cert_manager `csr_path_for` helper), frontend (api.js blob-download helper, views/settings.js).

- `[done] 2026-04-25 — Konsolideret create-flow: drop "Opret endpoint", brug registreringssiden til alt` — Den gamle desktop-create-side ([frontend/js/views/create.js](frontend/js/views/create.js)) duplikerede registrer-flowet fra 2.10.0; nu er den fjernet helt. Sidebar-linket "Mobil-registrering" er omdøbt til "Opret endpoint" og er nu eneste create-rute. `register.js` har fået AuthzVlan + AuthzACL-pickers så admin/editor har feature-paritet. Lag: frontend (slettet views/create.js, app.js route + import væk, index.html sidebar, register.js udvidet).

- `[done] 2026-04-25 — Chromeless mobil-registreringsside (registrar-UX-polish)` — Sidebar skjules helt på `/#register` for registrar-brugere og udloggede besøgende; login og logout bor nu på selve registreringssiden via en kompakt topbar med "Log ud"-knap. Admin/editor beholder sidebaren hvis de besøger ruten, så de stadig kan navigere væk. Bekræfter også at camera-barcode-scanning fra M8 virker fra det rene layout. Lag: frontend (app.js route-class, register view topbar, css `body.register-route`).

- `[done] 2026-04-21 — ACL-editor: fanger ISE's "src skal være any i alle ACE" før save` — ACL-editorens real-time syntaks-check udvidet til at afvise enhver ACE hvor source-feltet ikke er `any`. ISE gør det til en save-blocker med fejlen `"While creating DACL, the keyword 'Any' must be the source in all ACE in DACL"` — ISE erstatter selv `any` med klientens IP når DACL'en pushes til switchen. Fanges nu som `error`-severity i validerings-panelet mens brugeren skriver, så man ikke først ser det når man trykker Gem. Lag: backend (services/dacl_service).

- `[done] 2026-04-21 — Import fra CSV: valg for overskriv-vs-skip ved eksisterende endpoints` — Ny toggle "Ved eksisterende endpoint" i Import-view med to valg: **Skip** (default, samme som før — 409 fra ISE giver `skipped`) og **Overskriv** (bulk-import kalder `get_by_mac` + update for eksisterende i stedet for skip). Result-boksen viser nu også "Overwritten: N" sektion med MAC-liste. Backend: `BulkCreateRequest` fik `overwrite: bool = False`, `BulkResult` fik `overwritten: list[str]`, `bulk_create` forgrener på 409 baseret på flag. Lag: backend (schemas/endpoint, services/endpoint_service), frontend (api.js, views/import.js).

- `[done] 2026-04-21 — Sticky toolbar i Browse/Edit` — toolbar'en (Refresh, Export CSV, Kun portal, CoA reauth, Gem alle, server-filter, Kolonner, bulk-actions, page-size, count) i Browse/Edit er nu sticky øverst i content-området. Når man scroller ned i endpoint-tabellen bliver toolbar'en øverst så alle tools altid er tilgængelige uden scroll-op. Implementeret via `position: sticky; top: 0` på `.toolbar` + solid baggrund og `z-index: 20`. Negative margins lader toolbaren gå til kant-til-kant i card'et med en subtil bottom-border. Dark mode har matching baggrund. Lag: frontend (css).

- `[done] 2026-04-21 — Sticky sidebar — menu og status altid synlig` — Sidebar (menu + backend-status + user-info + logout) står nu fast og er altid synlig uanset hvor man scroller i content-området til højre. Implementeret ved at give `.app` `height: 100vh` og `.content` `overflow-y: auto`, så kun content-området scroller mens sidebar har fast højde. Menu fortsat øverst, status/user/logout nederst. Lag: frontend (css).

- `[done] 2026-04-21 — Auth-status farvning af række-checkbox i Browse/Edit` — når mindst ét filter er aktivt (portalOnly, kolonnefilter eller server-side MAC-filter), henter Browse/Edit en liste over MAC-adresser med aktiv RADIUS-session fra MnT (`/admin/API/mnt/Session/ActiveList` via nyt backend-endpoint `GET /api/endpoints/session-macs`) og farver række-checkboxen grøn (aktiv session = auth i access) eller rød (ingen aktiv session). Uden filter foretages ingen MnT-kald — farver vises kun når antallet af rækker er begrænset. Lag: backend (api/endpoints, services/endpoint_service), frontend (api.js, browse view, css).

- `[done] 2026-04-21 — PlatformType 1-til-1 raw→lokal mapping med CoA-binding` — PlatformType er igen et frit lokalt label sat af brugeren ("+ Tilføj"-input genskabt), men MnT-sync'en skriver ikke længere de rå ISE-værdier (airos/iosxe/...) direkte til endpoints. I stedet vedligeholder brugeren en 1-til-1 mapping på Attributter-siden, hvor hver kendt raw-værdi bindes til ét lokalt label *plus* en CoA-metode (`reauth` eller `disconnect`). MnT-sync oversætter raw → lokal label før den skriver til endpoint og rapporterer ikke-mappede raws separat (`unmapped_raw` + `skipped_unmapped`). CoA-on-save i Browse/Edit dispatcher nu reauth/disconnect via mapping-opslag (`{lokal_label: coa}`) i stedet for hardcoded `airos === disconnect`. Mappingen gemmes i `backend/platform_mapping.json` (gitignored). Lag: backend (core/platform_mapping_store, schemas/custom_attribute, services/custom_attribute_service, api/custom_attributes), frontend (api.js, attributes view, browse view).

- `[done] 2026-04-20 — PlatformType auto-sync fra ISE MnT + kanonisk værdiliste` — PlatformType er nu en lukket værdiliste (kanonisk: `airos`, `iosxe`, `iossw`, `nxos`, `meraki`). På Attributter-siden er fri-tekst-input fjernet for PlatformType; værdier opstår kun via to syncs: (1) ny "Sync platform fra MnT"-knap på sektionen henter aktive RADIUS-sessions fra `/admin/API/mnt/Session/ActiveList`, deriverer platform pr. session ud fra Cisco/Airespace/Meraki AVPair-markører + NAS-Port-Type fallback, og udfylder PlatformType pr. matchende endpoint (default kun tomme felter; checkbox "Overskriv eksisterende" tvinger re-derivation), (2) den eksisterende globale "Sync fra ISE"-knap canonicaliserer nu PlatformType — synonymer (catalyst9800/9800/wlc/aireos/nx-os/...) normaliseres til kanoniske værdier på endpoints, og helt ukendte værdier ryddes både på endpoints og i lokal store. Lag: backend (core/platform_types, ise/mnt_sessions, schemas/custom_attribute, services/custom_attribute_service, api/custom_attributes), frontend (api.js, attributes view).

- `[done] 2026-04-20 — PlatformType attribut + AireOS-aware CoA + kolonne hide/unhide` — nyt managed custom attribute "PlatformType" på endpoints (frie værdier som airos, iosxe, iossw, nxos osv.; administreres på Attributter-siden). Vises som ny "Platform"-kolonne i Browse/Edit, kan redigeres inline + i detail-modal + i bulk-edit, eksporteres/importeres via CSV (`CUSTOM.PlatformType`) og oprettes via Opret-formularen. Når global "CoA reauth"-toggle er TIL: hvis et gemt endpoint har `platformType == "airos"` sender portalen en CoA-Disconnect i stedet for CoA-Reauth (AireOS WLC honorerer ikke reauth pålideligt for policy-skift). Toolbar-knap "Kolonner ▾" giver hide/unhide pr. kolonne (persisteret i `localStorage`, default vis alle). Lag: backend (core/custom_attr_store, schemas/endpoint, services/endpoint_service), frontend (browse, create, import, attributes views, csv.js, css).

- `[done] 2026-04-20 — Persistente filtre i Browse/Edit` — alle aktive filtre i Browse/Edit (kolonnefiltre, server-side MAC-filter, "Kun portal"-toggle) gemmes i `localStorage` og restoreres når man vender tilbage til siden eller skifter mellem views. Filtrene fjernes kun ved aktiv handling (uncheck checkbox / ryd værdi / klik toggle). Lag: frontend (browse view).

- `[done] 2026-04-20 — AuthzACL attribut + Cisco IOS access-list editor` — nyt custom attribute "AuthzACL" på endpoints (dropdown med navne på DACL'er fra ISE; navngivet i samme stil som AuthzVlan). Ny sidebar-side "ACL" med editor for Cisco IOS-style access-list (DACL): liste over alle DACL'er fra ISE (både portal-oprettede og admin-oprettede), opret/rediger/slet, real-time syntaks-validering i backend (permit/deny + protocol + src/dst), endelig validering i ISE ved gem. Lag: backend (ise/dacls, services/dacl_service, schemas/dacl, api/dacls, schemas/endpoint, services/endpoint_service, custom_attr_store), frontend (api.js, app.js, views/dacls.js, views/browse.js, views/create.js, views/import.js, csv.js, index.html, css/styles.css).

- `[done] 2026-04-15 — Projekt bootstrap` — oprettet regel-filer, backend FastAPI skeleton, frontend skeleton. Lag: alle.
- `[done] 2026-04-15 — ISE endpoint listning` — hent liste af endpoints fra ISE 3.4 ERS API og vis i frontend tabel. Lag: backend (api, services, ise), frontend.
- `[done] 2026-04-15 — ISE endpoint group listning` — hent endpoint groups. Lag: backend (api, services, ise), frontend.
- `[done] 2026-04-15 — Health check endpoint` — `/api/health` til at verificere backend kører. Lag: backend (api).
- `[done] 2026-04-15 — Sidebar navigation` — venstre-menu med views: Opret / Import / Browse / Settings. Hash-baseret routing. Lag: frontend.
- `[done] 2026-04-15 — Opret endpoint view` — manuel indtastning af MAC, gruppe, beskrivelse. Validering af MAC format. Lag: frontend.
- `[done] 2026-04-15 — CSV import view` — upload fil eller paste CSV, preview med validering, bulk opret via backend. Lag: frontend, backend (api, services, ise).
- `[done] 2026-04-15 — Bulk create endpoints` — `POST /api/endpoints/bulk` der tager en liste og returnerer succeeded/failed. Lag: backend.
- `[done] 2026-04-15 — Browse/edit view` — liste endpoints, inline rediger beskrivelse, slet. Lag: frontend, backend (api PUT/DELETE).
- `[done] 2026-04-15 — Update endpoint endpoint` — `PUT /api/endpoints/{id}` generisk (description, group_id). Erstatter tidligere update_group. Lag: backend (ise, services, api).
- `[done] 2026-04-15 — Backend settings persistence` — `backend/config.json` override-fil. `GET/PUT /api/settings/backend` eksponerer ISE connection settings (url, user, password, api_type, verify_tls, timeout). Refresh af settings + reset af ISE client efter save. Lag: backend (core, schemas, services, api).
- `[done] 2026-04-15 — Settings view` — frontend form for backend ISE connection (password write-only) + frontend preferences (localStorage). Lag: frontend.

- `[done] 2026-04-19 — CoA Disconnect (deauthenticate)` — ny knap der udløser MnT `GET /admin/API/mnt/CoA/Disconnect/{psn}/{mac}/{disconnectType}` så klienten deautentificeres på WLC/switch og skal gen-associere. Brugbar når man vil tvinge ny DHCP (fx ved VLAN-skift). Per-endpoint knap i detail-modal + bulk-knap "Disconnect valgte". Admin kan konfigurere `coa_disconnect_type` i Settings (default 0 = DEFAULT deauth for wireless; 1 = port-bounce for wired; 2 = port-shutdown). Lag: backend (core, schemas, services, ise, api), frontend (api, settings, browse).

- `[done] 2026-04-19 — Browse/Edit refresh efter save` — efter hver succesful endpoint-save (detail-modal, Gem alle, Gem valgte) kaldes `load()` så tabellen genindlæses fra ISE. Filter- og portal-toggle-state bevares. Lag: frontend (browse view).

- `[done] 2026-04-19 — Global CoA reauth toggle i Browse/Edit` — toolbar-knap "CoA reauth: TIL/FRA" (persisteret i `localStorage`). Når TIL kaldes `POST /api/endpoints/{id}/coa-reauth` for hvert gemt endpoint efter save. Backend rammer ISE MnT `GET /admin/API/mnt/CoA/Reauth/{psn}/{mac}/{type}` — tvinger fornyet policy-evaluering så attribut-ændringer slår igennem uden at brugeren skal genforbinde. Admin kan konfigurere PSN-hostnavn og reauth type i Settings (default reauth_type=1 RERUN; tomt PSN afledes fra `ise_base_url`). Lag: backend (core, schemas, services, ise, api), frontend (api, settings view, browse view).

- `[done] 2026-04-19 — Slet attribut-værdi rydder også værdien i ISE` — `DELETE /api/custom-attributes/{attr}/values/{value}` scanner nu alle ISE-endpoints og rydder den givne værdi på alle endpoints der bruger den (feltet sættes til tomt; øvrige custom attributter bevares). Frontend-confirm advarer om opførslen og success-besked viser antal scannede/ryddede endpoints. Lag: backend (ise, services, schemas, api), frontend (attributes view).

- `[done] 2026-04-19 — Authentication + rollebaseret adgangskontrol` — lokal brugerbase i `backend/users.json` (PBKDF2-SHA256 med 600k iterations), stateless signerede tokens (HMAC-SHA256, 24h TTL, auto-genereret secret i `backend/auth_secret.key`). Tre roller: `admin` (alt), `editor` (CRUD på endpoints), `viewer` (kun GET). Første-gangs opsætning via login-siden når ingen brugere eksisterer. Ny "Brugere & roller"-sektion i Settings (admin-only) til CRUD på brugere. "Skift password"-sektion for alle. Sidebar viser kun ruter brugeren har adgang til. Lag: backend (core, schemas, services, api), frontend (auth.js, login view, app.js, api.js, settings view, index.html, css).

## Planlagte — Prioritet 1 (vigtigt nu)

- `[done] 2026-04-17 — Bulk throttling` — 150ms delay mellem ISE-kald i `bulk_create` for at overholde Ciscos 5–10 req/sec grænse. Forhindrer ERS overload ved store CSV imports. Lag: backend (services). Ref: [ISE_API_REFERENCE.md § Bulk](ISE_API_REFERENCE.md).
- `[done] 2026-04-18 — 409 Conflict → "skipped"` — ISE returnerer HTTP 409 når endpoint allerede eksisterer. Mappes nu til en `skipped` liste i `BulkResult` i stedet for `failed`, så bruger kan skelne fejl fra dubletter. Import-view viser tre spande. Lag: backend (services, schemas), frontend (import view).
- `[done] 2026-04-18 — Server-side filter/search` — `?search=` query parameter på `/api/endpoints`, `/endpoints/details` og `/details/all` oversættes til ERS `mac.CONTAINS.xxx` filter. Ny MAC-søgeboks i Browse-toolbaren (debounced) gør det muligt at finde endpoints uden at hente alle ISE-sider. Lag: backend (api, ise, services), frontend (api, browse view, css).
- `[done] 2026-04-18 — Parse Location header efter POST` — ERS returnerer `Location: .../endpoint/{uuid}` ved 201 Created. Parses nu i `IseEndpointRepository.create` så det nye id returneres direkte uden follow-up GET. `POST /api/endpoints` svarer med `{"status":"created","id":"<uuid>"}`. Lag: backend (ise client, endpoints repository, services, api).
- `[done] 2026-04-18 — ISE connectivity test` — `POST /api/settings/test` laver en autenticeret GET mod ISE (endpoint groups, size=1) med valgfrit medsendte eller aktive settings. Ny "Test forbindelse"-knap i Settings viser success/latency eller fejl uden at gemme. Lag: backend (schemas, services, api), frontend (api, settings view).
- `[done] 2026-04-16 — CSV export template` — erstat hardkodet 100+ kolonner med brugerdefinerbar CSV-template (default: 34 kolonner fra ISE). Template kan importeres fra CSV-fil (header-only) og persisteres i localStorage. Lag: frontend (csv.js, settings view).
- `[done] 2026-04-16 — Identity Group "ingen" → Unknown` — "— ingen —" i Browse/Edit group-dropdown flytter endpoint til Unknown-gruppen og disabler staticGroupAssignment. Kolonne omdøbt til "Identity Group". Lag: backend (schemas, ise, services), frontend (browse view).
- `[done] 2026-04-17 — Custom attribute "Type" + Attributter-side` — nyt custom attribute "Type". Ny sidebar-side "Attributter" til at administrere værdier for Type, Owner, Lokation, AuthzVlan. Lag: backend (core, schemas, ise, services), frontend (ny view, sidebar, create, browse, import, csv).
- `[done] 2026-04-17 — HypervisionISEPortal skjult attribut + filter` — usynligt custom attribute "HypervisionISEPortal" sættes automatisk ved oprettelse og redigering. Toggle-knap i Browse/Edit for at vise kun portal-endpoints vs. alle. Lag: backend (core, ise, services), frontend (browse view).
- `[done] 2026-04-17 — Bulk select + bulk actions i Browse/Edit` — fjern individuelle Save/Del knapper per række. Tilføj checkbox per række + global select-all i header. Bulk-handlinger (Gem valgte / Slet valgte) i toolbar. Lag: frontend (browse view, css).
- `[done] 2026-04-17 — Global Gem alle-knap i Browse/Edit` — global save-knap i toolbar ved siden af Refresh/Export/Kun portal. Tracker dirty-state per række og gemmer alle ændrede endpoints med ét klik. Lag: frontend (browse view, css).
- `[done] 2026-04-17 — Bulk Rediger valgte i Browse/Edit` — "Rediger valgte" knap der åbner en modal til at sætte værdier (group, description, type, owner, lokation, authzvlan) på alle valgte endpoints på én gang. Lag: frontend (browse view, css).
- `[done] 2026-04-17 — Concurrent endpoint detail fetch` — parallelisér GET af individuelle endpoints med asyncio.gather + semaphore (max 5 samtidige). Reducerer load-tid markant. Lag: backend (services).
- `[done] 2026-04-18 — Export kun valgte endpoints` — Export CSV i Browse/Edit eksporterer nu kun de valgte endpoints hvis noget er selekteret. Hvis ingen er valgt, eksporteres alle (filtrerede) som før. Lag: frontend (browse view).
- `[done] 2026-04-18 — Portal-default CSV template + auto-extend ved ISE import` — "Nulstil"-knap i CSV Export Template giver nu kun portalens egne kolonner (MAC, IdentityGroup, Description, StaticGroupAssignment, CUSTOM.Type/Owner/Lokation/AuthzVlan/HypervisionISEPortal) i stedet for det store 34-kolonne ISE-template. Når en ISE-template importeres, udvides den automatisk med portal-kolonner der mangler, så export aldrig taber portal-data. Lag: frontend (csv.js, settings view).

## Planlagte — Prioritet 2 (bør have)

- `[done] 2026-04-18 — Endpoint detalje-view` — klik på MAC-linket i Browse åbner en modal der henter fuld `GET /api/endpoints/{id}` med alle felter (groupId, profileId, portalUser, identityStore, customAttributes). Inline edit af description/group/type/owner/lokation/authzvlan + Gem-knap. Lag: frontend (browse view, css), backend (schemas, services).
- `[done] 2026-04-18 — ERS filter-operatorer i Browse` — felt-dropdown (MAC/Name/Description) + operator-dropdown (CONTAINS/EQ/NEQ/STARTSW/ENDSW) + værdi-input (debounced) bygger ERS filter-expression og sender den til backend som gentagelig `?filter=` query. Backend tager filter-liste videre til ISE. Lag: frontend (browse view, css, api), backend (api, services).
- `[done] 2026-04-17 — Gruppevalg i Browse/edit` — dropdown per række flytter endpoint til en anden gruppe via PUT /api/endpoints/{id}. Inkluderet i fuld Browse/Edit + bulk-save. Lag: frontend (browse view).
- `[done] 2026-04-17 — Multi-page pagination` — Browse view med forrige/næste paginering + page size selector direkte i toolbar. Backend returnerer total count via `PaginatedEndpointDetails`. Lag: frontend, backend.
- `[done] 2026-04-18 — Open API support` — parallel integration mod `/api/v1/endpoint` implementeret i `app/ise/openapi_endpoints.py` (list/get/create/update/delete + `endpoint-identity-group`). Response-shapes normaliseres til ERS-form så service-laget deler kode. `EndpointService.__init__` dispatcher baseret på `config.settings.ise_api_type`. `/api/settings/test` prober korrekt endpoint pr. api_type. Lag: backend (ise, services, settings). Ref: [ISE_API_REFERENCE.md § Open API](ISE_API_REFERENCE.md).

## Planlagte — Prioritet 3 (nice to have)

- `[planned] — ANC quarantine actions` — "Quarantine" og "Clear" knapper per endpoint i Browse view. Bruger ERS `/ers/config/ancendpoint/apply` og `/clear`. Lag: backend (ise, services, api), frontend.
- `[done] 2026-04-16 — ISE-kompatibel CSV import/export` — import forstår ISE Context Visibility CSV-format (MACAddress, IdentityGroup, Description, CUSTOM.* kolonner) + simpelt format. Export fra Browse genererer ISE-kompatibelt CSV med alle 100+ kolonner. Fælles CSV-modul i `js/csv.js`. Lag: frontend (csv.js, import view, browse view).
- `[done] 2026-04-16 — Fuld Browse/Edit med alle felter` — browse-tabellen viser MAC, Group (dropdown), Description, Owner, Lokation, AuthzVlan med fuld inline redigering af alle felter. Filter søger i alle felter. Group valgfri ved oprettelse.
- `[done] 2026-04-16 — Custom attributes i Browse/Edit view` — henter fuld endpoint-detalje inkl. custom attrs fra ISE. Nye routes: `GET /api/endpoints/details`, `GET /api/endpoints/{id}`. Lag: backend (api, services, schemas), frontend (api, browse view).
- `[done] 2026-04-16 — Installations- og driftsdokumentation` — INSTALL.md med forudsætninger, opsætning, konfiguration, brug af alle views, API-reference, logning, fejlsøgning, drift og sikkerhed. Lag: docs.
- `[done] 2026-04-16 — Custom endpoint attributes (Owner, Location, AuthzVlan)` — dropdown-lister i Opret endpoint view med mulighed for at tilføje nye værdier. CSV import understøtter custom attr kolonner. Backend: lokal registry (`custom_attr_values.json`), ISE sync, attribute definition management. Lag: backend (core, ise, schemas, services, api), frontend (api, create view, import view).
- `[planned] — SGT (Security Group Tag) tildeling` — vis/ændr `securityGroupId` per endpoint. Kræver lookup mod `/ers/config/sgt`. Lag: backend (ise, services), frontend.
- `[done] 2026-04-17 — Dark mode` — theme toggle i Settings (light/dark) gemt i localStorage. `applyTheme`/`initTheme` i [settings.js](frontend/js/views/settings.js) sætter `data-theme="dark"` på `<html>`; CSS-variationer i [styles.css § Dark theme](frontend/css/styles.css). Lag: frontend.
- `[done] 2026-04-18 — Export til CSV` — "Export CSV" knap i Browse/Edit toolbar eksporterer selekterede, filtrerede eller alle endpoints (auto-hentning fra ISE hvis ingen selektion og intet filter). ISE-kompatibelt format via `toIseCsv()` fra `csv.js`. Lag: frontend.
- `[done] 2026-04-19 — Audit log view` — ny sidebar-side "Log" der læser `backend/logs/app.log` via `GET /api/logs` og viser parsede entries (timestamp, niveau, logger, besked) i en filtrerbar tabel. Niveau- og fritekst-filter, refresh-knap, farvekodet niveau. Lag: backend (api), frontend.
